<!-- record-meta
{
  "record_id": "20260921-125459-223bae5",
  "experiment": "digital-erc",
  "supersedes": null,
  "git_revision": "223bae56c85af608e12d562c885d32ce3c7e7edf",
  "provenance": {
    "klt_version": "0.5.0+g1d964cf4ed93",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b (not resolved by klt erc itself; the local install's own SOURCES marker confirms the pinned revision, and the spec's layer numbers were verified against that install's gf180mcu.lyp)"
    },
    "deck": {
      "name": "n/a",
      "content_hash": "n/a"
    },
    "inputs": [
      { "path": "layout/digital/usb_utmi_phy.gds", "content_hash": "sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476" },
      { "path": "layout/digital/erc-supply-spec.json", "content_hash": "sha256:045642b270ded0597b9e3e4343c3ed7a3d28332b13c99e6edef1868324d6a61a" },
      { "path": "flow/request-usb-utmi-phy-par.json", "content_hash": "sha256:5e267e8645ade958cadc62e93ab523278d0f84c8ef9e8f1a123e1442036e27e2" },
      { "path": "verification/records/place-and-route/artifacts/20260825-224709-6a83263/place-and-route-output.json", "content_hash": "sha256:9d06d9dafa141f67bb26417aa07c112f4df4c8bfaa1b84a6d4b0abfe0e4581e6" },
      { "path": "verification/records/digital-lvs/artifacts/20260825-224930-6a83263/lvs-report.json", "content_hash": "sha256:1c2ad31d5911c2f30a23a70be21f1da2dab340c2f75adf8a15ffa38836f512ba" }
    ]
  }
}
-->

# Record 20260921-125459-223bae5

- **Record ID**: 20260921-125459-223bae5
- **Claim**: **the routed digital PHY's two declared supplies each resolve
  to exactly one electrical island, with no short between them** — the
  `klt erc` supply half of T1 checklist item 11 (power delivery,
  structural) for the digital partition, against the routed GDS
  `layout/digital/usb_utmi_phy.gds` with the committed supply spec
  `layout/digital/erc-supply-spec.json` (issue #77). Concretely: zero
  `erc.unconnected_net` and zero `erc.supply_short` naming `VDD` or `VSS`.
  Per the item's own text, the graded subject is the `erc_findings`
  supply rules, **not** the report's overall `status` (an antenna verdict
  does not block this item, #1994) — and this run's `status` is
  `not_checked` for exactly that reason (see Result).
- **Design provenance**: layout (post-P&R, routed GDS — the same frozen
  artifact as the digital-DRC and digital-LVS records, verbatim the file
  the ERC run hashed) plus the supply spec (this record pins the spec's
  content hash, so a spec edit invalidates this record and demands a
  re-run — that is the point of `provenance.spec.content_hash`).
- **Run configuration**:
  ```
  klt erc layout/digital/usb_utmi_phy.gds \
    layout/digital/erc-supply-spec.json \
    --top usb_utmi_phy --format json
  ```
  No `--pdk`: `klt erc` carries an antenna-ratio limit table for sky130
  only, so there is nothing to compare gf180mcu ratios against (see
  Result for how that surfaces). The spec declares: `stackup[0]` Poly2
  `30/0` with `"role": "gate"` and `active_layer` Comp `22/0` (so
  `poly ∩ diff` is the gate-area denominator and the 854
  filltie/endcap/filler poly bodies stay out of `gates[]`); Metal1-Metal5
  `34/0`, `36/0`, `42/0`, `46/0`, `81/0` with `label_layer` on Metal1
  `34/10` (64 `VDD` + 64 `VSS` standard-cell PG pin labels, read from
  this GDS's own text inventory) and Metal5 `81/10` (exactly one `VDD` +
  one `VSS` PDN pin label); `vias[]` Contact `33/0`, Via1 `35/0`, Via2
  `38/0`, Via3 `40/0`, Via4 `41/0`; `nets[]`: `VDD` and `VSS`, both
  `"kind": "supply"`. Layer numbers were derived from the PDK's own
  `libs.tech/klayout/tech/gf180mcu.lyp` and cross-checked against
  klayout-tools' curated gf180mcu deck layer table — identical — rather
  than copied from another PDK's spec. There is **no `ties[]` section**,
  deliberately (see Result).
  **Tool note**: this run used `klt 0.5.0+g1d964cf4ed93` — klayout-tools
  commit `1d964cf4ed93b15bd36acf1eb14e3855b3c500d1` (built from a clean
  clone at that exact commit) — because the documented `klt erc` evidence
  envelope (`provenance` with `input`/`spec` content hashes, `status`,
  `erc_status`, `erc_coverage`) postdates both this repo's pinned klt
  revision and every klayout-tools tag including v0.5.0, none of which
  emit those fields. Filed as friction:
  [klayout-tools#2238](https://github.com/2AMLogic/klayout-tools/issues/2238).
  The input hash this report carries was independently re-derived
  locally (`shasum -a 256 layout/digital/usb_utmi_phy.gds`) and matches.
- **Statistical convention**: N/A — a single deterministic connectivity
  extraction, no randomized element.
- **Result**:
  - **`erc_status: "clean"`, `erc_finding_count: 0`.** Zero
    `erc.unconnected_net` and zero `erc.supply_short` naming `VDD` or
    `VSS` — each declared supply resolves to exactly one electrical
    island (the rule fires on zero matches *and* on more-than-one, so
    zero findings is the one-island verdict, not an absence), and the two
    supplies are not shorted to each other. The full findings array is
    empty: no `erc.multiply_driven_net` between a supply and anything
    else, and no `erc.floating_gate` among the 951 gate nets the
    extraction found.
  - **`status: "not_checked"`, process exit `4`** — the antenna half of
    the envelope, and the expected outcome on this PDK: with no gf180mcu
    antenna table to compare against, every `levels[]` verdict is
    `"unchecked"`, and the coverage rollup correctly refuses to call
    known-zero checked antenna work a pass. This record's claim is the
    connectivity half, whose `clean` verdict needs no PDK table —
    `docs/cli/erc.md` ("Two verdicts") gates a structural-supply check on
    `erc_status`, and so does this record. **This record is not an
    antenna-clean claim** for this layout.
  - **`erc_coverage` (machine-readable)**: `checked` carries
    `erc.floating_gate` for all 951 gate nets plus
    `erc.net_connectivity:["VDD"]` and `erc.net_connectivity:["VSS"]` —
    the two declared supplies were actually checked, not skipped;
    `skipped` is empty; `inapplicable` carries exactly one entry:
    `{"id": "erc.missing_tie:[]", "reason": "no_ties_declared"}`.
  - **`erc.missing_tie` is not computed — an absence of evidence, not
    evidence of absence.** The spec deliberately declares no `ties[]`
    (klayout-tools#2169: on a real routed standard-cell layout the
    pre-#2166 well-conduction model collapses the design into one island
    and reports a **false** `erc.supply_short`; gf180-drone-fc's
    FRICTION F-034 reproduces it four ways), so the rule never ran —
    which the envelope itself states via the `no_ties_declared`
    inapplicable entry above. The well-tie evidence that *does* stand in
    for it, named per issue #77: `power.pdn: true` with
    `power.tapcell_master: gf180mcu_fd_sc_mcu9t5v0__filltie` and
    `global_connect: true` in the committed place-and-route envelope; the
    routed DEF's real `SPECIALNETS` VDD/VSS grid (per the digital-DRC
    and digital-LVS records of the same run); and this GDS's own drawn
    tap evidence — 62 `VNW` + 62 `VPW` well-tap labels (layers 21/10,
    204/10) beside the 64+64 VDD/VSS PG pin labels.
  - **Item 11's kind-dependent digital legs, read against committed
    evidence**: `power.pdn: true` with a named `tapcell_master` — met
    (place-and-route envelope above); every `power.straps[].layer`
    covered by this spec's stackup — met (straps on Metal1, Metal4,
    Metal5; all three are stackup roles); LVS `power_connectivity.status`
    reading `"match"` — **not met**: the committed digital-LVS record
    predates that field (`klt` 0.3.0; the block is absent from its
    envelope), and for item 11's digital column neither `"unchecked"`
    nor absent satisfies the item. That gap is now tracked as
    [#79](https://github.com/2AMLogic/gf180-usb2-phy/issues/79) —
    until it closes, item 11 stays **partially met** at the tracker
    level: the ERC supply read passes, the PDN/tapcell/straps evidence
    passes, the LVS power/ground verdict leg does not exist yet.
  - **Overall: pass** for the claim as scoped (the `erc_findings` supply
    rules over the two declared nets).
- **klt provenance**: `klt` `0.5.0+g1d964cf4ed93` (klayout-tools commit
  `1d964cf4ed93b15bd36acf1eb14e3855b3c500d1`, built from a clean clone at
  that commit — see Run configuration), `klayout` 0.30.12.
  `provenance.input.content_hash`
  `sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476`
  — the routed GDS, byte-identical to the committed file (re-derived
  locally and matched). `provenance.spec.content_hash`
  `sha256:045642b270ded0597b9e3e4343c3ed7a3d28332b13c99e6edef1868324d6a61a`
  — the committed supply spec. `provenance.pdk` is `null` in the envelope
  itself (no `--pdk` was given); the record's own `provenance.pdk` above
  names the gf180mcuD install the layer cross-check read, pinned per
  `docs/environment-setup.md`.
- **Links**:
  - Supply spec (committed, this record's subject):
    `layout/digital/erc-supply-spec.json`
  - Layout under test: `layout/digital/usb_utmi_phy.gds`
  - Raw klt JSON envelope (frozen):
    `artifacts/20260921-125459-223bae5/erc-supply-report.json`
  - Place-and-route leg that produced this GDS (and the PDN/tapcell
    evidence this claim cites):
    `verification/records/place-and-route/records/20260825-224709-6a83263.md`
  - DRC leg against the same GDS:
    `verification/records/digital-drc/records/20260825-224815-6a83263.md`
  - LVS leg against the same GDS (signal half; predates
    `power_connectivity` — see the item-11 leg above):
    `verification/records/digital-lvs/records/20260825-224930-6a83263.md`
  - Follow-up filed for the missing item-11 LVS leg: issue
    [#79](https://github.com/2AMLogic/gf180-usb2-phy/issues/79)
  - Friction filed (documented `klt erc` envelope unreleased past any
    tag; this run had to pin a `main`-branch commit by hand):
    [klayout-tools#2238](https://github.com/2AMLogic/klayout-tools/issues/2238)
  - Worked example this spec follows:
    gf180-drone-fc's `layout/digital/erc-supply-spec.json` and
    `layout/digital/README.md` (structural power delivery for a digital
    flow), per issue #77's own pointer.
- **Timestamp / author**: 2026-09-21T12:54:59Z, loom-builder (issue #77)
- **Supersedes**: none — first record in this experiment directory.
