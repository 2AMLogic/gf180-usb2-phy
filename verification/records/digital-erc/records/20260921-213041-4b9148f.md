<!-- record-meta
{
  "record_id": "20260921-213041-4b9148f",
  "experiment": "digital-erc",
  "supersedes": "20260921-125459-223bae5",
  "git_revision": "4b9148f2886564658ef742f09eda65ea0798990b",
  "provenance": {
    "klt_version": "0.5.0+ge8ca621a6961",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b (not resolved by klt erc itself — no --pdk given; the tie layer numbers were verified against this install's own libs.tech/klayout/lvs/rule_decks/layers_definitions.lvs, pinned per docs/environment-setup.md)"
    },
    "deck": {
      "name": "n/a",
      "content_hash": "n/a"
    },
    "inputs": [
      { "path": "layout/digital/usb_utmi_phy.gds", "content_hash": "sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476" },
      { "path": "layout/digital/erc-supply-spec.json", "content_hash": "sha256:194c781ba27426de010030732e85ee75b34548bafa8b20af003184fb94145fef" },
      { "path": "flow/request-usb-utmi-phy-par.json", "content_hash": "sha256:5e267e8645ade958cadc62e93ab523278d0f84c8ef9e8f1a123e1442036e27e2" },
      { "path": "verification/records/place-and-route/artifacts/20260825-224709-6a83263/place-and-route-output.json", "content_hash": "sha256:9d06d9dafa141f67bb26417aa07c112f4df4c8bfaa1b84a6d4b0abfe0e4581e6" },
      { "path": "verification/records/digital-lvs/artifacts/20260921-145814-5bee855/lvs-report.json", "content_hash": "sha256:bc3df97300d3de831afad9b21a4faa1ccee6557f871baaf7c2cb4d396a483e52" }
    ]
  }
}
-->

# Record 20260921-213041-4b9148f

- **Record ID**: 20260921-213041-4b9148f
- **Claim**: **the routed digital PHY's two declared supplies each resolve
  to exactly one electrical island, are not shorted to each other, and
  every drawn N-well / LVPWELL region carries a checked, connected tie to
  its declared supply** — the full `klt erc` supply half of T1 checklist
  item 11 (power delivery, structural) for the digital partition, against
  the routed GDS `layout/digital/usb_utmi_phy.gds` with the ties-augmented
  supply spec `layout/digital/erc-supply-spec.json` (issue #83).
  Concretely: zero `erc.unconnected_net`, zero `erc.supply_short`, and
  zero `erc.missing_tie`, with both `ties[]` entries reported in
  `erc_coverage.checked` (not `skipped`, not `inapplicable` — the
  klayout-tools#2199 rule that a degenerate or skipped tie renders zero
  for a reason unrelated to taps). As before, the graded subject is the
  `erc_findings` supply rules, not the report's overall antenna `status`.
- **Design provenance**: layout (post-P&R, routed GDS — byte-identical to
  the file the superseded record hashed; `provenance.input.content_hash`
  re-derived locally with `shasum -a 256` and matched) plus the supply
  spec, whose content hash this record pins — the ties-augmented spec is
  the delta that demanded this re-run.
- **Run configuration**:
  ```
  klt erc layout/digital/usb_utmi_phy.gds \
    layout/digital/erc-supply-spec.json \
    --top usb_utmi_phy --format json
  ```
  No `--pdk`, no `--deck` — same shape as the superseded run. The spec
  is unchanged from `20260921-125459-223bae5` in its stackup (Poly2
  `30/0` gate role with `active_layer` Comp `22/0`; Metal1–Metal5 with
  `label_layer` on `34/10` and `81/10`), `vias[]` (Contact/Via1–Via4),
  and `nets[]` (`VDD`/`VSS`, both `"kind": "supply"`). **The one change:
  a `ties[]` section, now declared** (issue #83) because the upstream
  blocker that forced its omission — klayout-tools#2169's tie-graph
  collapse, fixed by klt PR #2186 (merge `62fb705ca2`) — is a strict
  ancestor of this repo's pinned klt `e8ca621a` (verified live:
  `compare/62fb705ca2...e8ca621a` → `ahead_by=27, behind_by=0`). The
  two entries follow klt erc's own gf180mcu guidance (tap = the boolean
  `Comp ∩ Nplus` / `Comp ∩ Pplus`):
  - `nwell_vdd_tap`: `well_layer` Nwell `21/0`, `tap_layer` Comp `22/0`,
    `tap_requires` Nplus `32/0`, `connect_to` `Metal1`, `net` `VDD`.
  - `lvpwell_vss_tap`: `well_layer` LVPWELL `204/0`, `tap_layer` Comp
    `22/0`, `tap_requires` Pplus `31/0`, `connect_to` `Metal1`, `net`
    `VSS`.
  Well/tap layer numbers are the PDK's own (`layers_definitions.lvs`:
  `nwell = get_polygons(21, 0)`, `lvpwell = get_polygons(204, 0)`,
  `pplus = get_polygons(31, 0)`, `nplus = get_polygons(32, 0)`,
  `comp = get_polygons(22, 0)`), cross-checked against klayout-tools'
  curated gf180mcu deck table — identical. `tap_requires` is
  load-bearing on both entries: a bare Comp tap matches every
  source/drain contact in the same well, which is exactly the
  degenerate-declaration shape klt#2199 grades as skipped.
  **Tool note**: this run used the repo's pinned klt
  (`0.5.0+ge8ca621a6961`, `docs/environment-setup.md`) — unlike the
  superseded record, no hand-pinned unreleased commit was needed; the
  documented envelope (post-#2186 tie isolation, post-#2199
  degenerate-tie reporting) is in the pin.
- **Statistical convention**: N/A — a single deterministic connectivity
  extraction, no randomized element.
- **Result**:
  - **`erc_status: "clean"`, `erc_finding_count: 0`.** Zero
    `erc.unconnected_net` and zero `erc.supply_short` naming `VDD` or
    `VSS` (each declared supply still resolves to exactly one electrical
    island, unchanged from the superseded run — post-#2169 tie isolation
    guarantees a `ties[]` declaration cannot alter `nets[]` findings),
    **and zero `erc.missing_tie`**: every merged Nwell and every merged
    LVPWELL shape carries a `Comp ∩ Nplus` / `Comp ∩ Pplus` tap that
    reaches VDD / VSS respectively through Metal1.
  - **Both ties are graded as checked work, not skips**:
    `erc_coverage.checked` carries `erc.missing_tie:["nwell_vdd_tap"]`
    and `erc.missing_tie:["lvpwell_vss_tap"]` (plus
    `erc.net_connectivity:["VDD"]` / `["VSS"]` and the 951
    `erc.floating_gate` gate nets); `skipped` is **empty** — no
    `degenerate_tap_declaration` (klt#2199) — and `inapplicable` is
    **empty** — the superseded run's
    `{"id": "erc.missing_tie:[]", "reason": "no_ties_declared"}` entry
    is gone because ties are now declared. This is the difference the
    item-11 grading rule (klt signoff) reads: a zero arriving from a
    skipped or uncomputed check does not satisfy the criterion.
  - **`status: "not_checked"`, process exit `4`** — the antenna half,
    unchanged in shape from the superseded run: `klt erc` carries an
    antenna-ratio limit table for sky130 only, so on gf180mcu every
    `levels[]` verdict is `"unchecked"`. **This record is not an
    antenna-clean claim**, and an antenna verdict does not block item 11
    (#1994).
  - **Standing-in tap evidence, re-measured**: the committed
    place-and-route envelope still carries `power.pdn: true` with
    `power.tapcell_master: gf180mcu_fd_sc_mcu9t5v0__filltie`, and this
    GDS's flat text inventory carries 1115 `VNW` labels on `21/10` and
    1115 `VPW` labels on `204/10` beside the 1206+1206 VDD/VSS PG pin
    labels on `34/10`. (Counting-method note: the superseded record and
    issue #77's spec comment said "62 + 62"; a direct flat
    `begin_shapes_rec` count after `flatten(-1)` measures 1115 each.
    Both describe the same drawn tap stacks — the earlier figure counted
    a coarser merge — and neither count feeds the graded check, which
    reads the tie geometry itself, not labels.)
  - **Item 11.digital, read against committed evidence after this
    record**: the ERC supply rules half is now fully computed and clean
    (this record); the PDN/tapcell half is met by the committed
    place-and-route envelope; the LVS `power_connectivity` half is met
    by the Sep-21 gate-level LVS record (#79 / PR #82, `status:
    "match"`). The re-rendered signoff report (this change) grades
    item 11.digital **`met`**.
  - **Overall: pass** for the claim as scoped.
- **klt provenance**: `klt` `0.5.0+ge8ca621a6961` (klayout-tools commit
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa` — the repo pin per
  `docs/environment-setup.md`, installed from a clean clone at that
  exact commit), `klayout` 0.30.12.
  `provenance.input.content_hash`
  `sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476`
  — the routed GDS, byte-identical to the committed file (re-derived
  locally and matched). `provenance.spec.content_hash`
  `sha256:194c781ba27426de010030732e85ee75b34548bafa8b20af003184fb94145fef`
  — the ties-augmented supply spec (re-derived locally and matched).
  `provenance.pdk` is `null` in the envelope itself (no `--pdk` was
  given); this record's own `provenance.pdk` above names the gf180mcuD
  install the layer cross-check read, pinned per
  `docs/environment-setup.md`.
- **Links**:
  - Supply spec (committed, this record's subject):
    `layout/digital/erc-supply-spec.json`
  - Layout under test: `layout/digital/usb_utmi_phy.gds`
  - Raw klt JSON envelope (frozen):
    `artifacts/20260921-213041-4b9148f/erc-supply-report.json`
  - Superseded record (the no-ties absence-of-evidence state this
    record replaces): `records/20260921-125459-223bae5.md`
  - Place-and-route leg (PDN/tapcell evidence + this GDS):
    `verification/records/place-and-route/records/20260825-224709-6a83263.md`
  - LVS leg with the computed `power_connectivity` verdict (#79):
    `verification/records/digital-lvs/records/20260921-145814-5bee855.md`
  - Upstream tie-isolation fix (in the pin):
    [klayout-tools#2169](https://github.com/2AMLogic/klayout-tools/issues/2169)
    via [klt#2186](https://github.com/2AMLogic/klayout-tools/pull/2186);
    degenerate-tie grading:
    [klt#2199](https://github.com/2AMLogic/klayout-tools/issues/2199)
  - Issue: [#83](https://github.com/2AMLogic/gf180-usb2-phy/issues/83)
- **Timestamp / author**: 2026-09-21T21:30:41Z, loom-builder (issue #83)
- **Supersedes**: `20260921-125459-223bae5` — that record's
  `erc.missing_tie` "not computed / no_ties_declared" state was an
  honest absence of evidence forced by the then-unfixed upstream
  klayout-tools#2169 defect; this record replaces it now that the fix
  is inside the pinned klt and the spec declares its ties. Everything
  that record claimed about the supplies themselves (one island each,
  no short) is re-confirmed unchanged here.
