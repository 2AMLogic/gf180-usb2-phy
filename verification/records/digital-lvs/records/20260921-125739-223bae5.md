<!-- record-meta
{
  "record_id": "20260921-125739-223bae5",
  "experiment": "digital-lvs",
  "supersedes": "20260825-224930-6a83263",
  "git_revision": "223bae56c85af608e12d562c885d32ce3c7e7edf",
  "provenance": {
    "klt_version": "0.5.0+ge8ca621a6961",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637",
      "released": false
    },
    "inputs": [
      { "path": "layout/digital/usb_utmi_phy.gds", "content_hash": "sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476" },
      { "path": "layout/digital/usb_utmi_phy_routed.v", "content_hash": "sha256:c1e0e8ed6e887ed1e45d1371a69e23f23fd3d5344dc73e9c2e963b3d15e6fc87" },
      { "path": "scripts/digital_lvs.py", "content_hash": "sha256:fef15d1f1ec431c1d83fae5537b44559aa85ea7b15ba8cb32134ec7797378310" },
      { "path": "flow/request-usb-utmi-phy-par.json", "content_hash": "sha256:5e267e8645ade958cadc62e93ab523278d0f84c8ef9e8f1a123e1442036e27e2" }
    ]
  }
}
-->

# Record 20260921-125739-223bae5

- **Record ID**: 20260921-125739-223bae5
- **Claim**: re-runs the superseded record's exact claim — **gate-level LVS
  on the digital PHY is a match**, `layout/digital/usb_utmi_phy.gds`
  instantiates exactly the standard cells the as-built gate-level netlist
  `layout/digital/usb_utmi_phy_routed.v` names, wired to exactly the same
  nets, with the same top-level pin set — with every design input
  byte-identical to the superseded run and exactly two things changed: the
  `klt` pin (this repo's re-pin to
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`, 58 commits past the
  `klt`-side item-11 work this manifest era rides on — see
  `docs/environment-setup.md`'s re-pin history), and the `klayout` engine
  (0.30.10, the version that pin reports as tested). The result must
  reproduce `status: "match"` for the superseded record to stand as more
  than a one-revision artifact. It does.
- **Design provenance**: layout (post-P&R, **extracted**) vs. netlist
  (post-P&R as-built, `write_verilog` — CTS buffers, resizes and antenna
  repair included) — unchanged from the superseded record; the extraction
  input hash below is still `sha256:341ccb…`, the committed GDS.
- **Run configuration**:
  ```
  source .venv/bin/activate            # klt 0.5.0+ge8ca621a6961, klayout 0.30.10
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py --negative-control
  ```
  Same committed driver (`scripts/digital_lvs.py`, hash above), same
  `klt extract` → `klt lvs` chain, same abstracted-cell policy, same
  negative control, as the superseded record.
- **Statistical convention**: N/A — a single deterministic compare of a
  fixed netlist pair, plus one deterministic negative control, same as the
  superseded record.
- **Result**:
  - **`status: "match"`, `mismatch_count: 0`, `error_count: 0`.**
  - Counts: nets layout 373 / reference 373 (685 matched); pins 37 / 37
    (349 pin-correspondences matched); devices 0 / 0 — by construction,
    every transistor is inside an abstracted cell, so the compare is over
    subcircuit instances and net topology. Inventory reconciled: 352
    logic instances across 55 cell types on both sides; 35 top-level RTL
    ports plus `VDD`/`VSS`.
  - **Negative control**: the deliberately broken reference is correctly
    rejected — `status: "mismatch"`, one `category: "topology"`,
    `severity: "error"` entry ("subcircuit instance could not be matched
    to a counterpart"). The compare still measures something at the new
    pin.
  - **Overall: pass** — the superseded record's verdict reproduces at the
    re-pinned toolchain, with all four `provenance.inputs[]` hashes
    byte-identical to it.
  - **What changed in the envelope, and why this record exists**: the new
    `lvs-report.json` records `provenance.input.content_hash` =
    `sha256:c6819988…` (`role: "netlist"` — the extracted layout-side
    SPICE), which `klt lvs` did not populate before klayout-tools#1969.
    That is what makes a `klt signoff --manifest` "LVS clean" citation
    **pinnable** (`docs/cli/signoff.md`: pre-#1969 envelopes grade
    `unverifiable_provenance` when pinned) — `manifests/gf180-usb2-phy.json`
    item 4 cites this record's envelope. The compare itself changed
    nothing: `environment.layout_sha256` here is the hash of the
    freshly-extracted `usb_utmi_phy_layout.spice` for this exact GDS, and
    the extract envelope's own input hash is still the committed GDS.
- **klt provenance**: `klt` 0.5.0+ge8ca621a6961 (git revision
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`), `klayout` 0.30.10 — the
  version that `klt`-pin reports as tested, so
  `klayout_version_mismatch` is `false` (the first freeze attempt at
  klayout 0.30.12 flagged `true` and was discarded, not committed).
  Extraction deck `gf180mcu`, content_hash
  `sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637`,
  `released: false` — the deck content differs from the superseded
  record's (`sha256:79e71a1e…`, `released: true`) because the re-pin moved
  263 commits past the old pin and the deck shipped with the tool moved
  with it; the verdict was re-derived from scratch against whatever deck
  that pin ships, which is the point of a re-measure rather than a hash
  transplant. PDK `gf180mcuD`, open_pdks
  `c6d73a35f524070e85faff4a6a9eef49553ebc2b`, resolved from `PDK_ROOT`.
  Compared netlist hashes (`environment` block): layout
  `c6819988aee1a2ec59cf6fb8e9e03baf9135d589203c7d1c9ebad0040351c2af`,
  reference
  `55078e6ac9c5f5ed12b076748730a9d0b6cd38650bd6c2e755e195f96952a7cc`
  (the reference generator being deterministic from the same Verilog, the
  reference hash is byte-identical to the superseded record's).
- **Links**:
  - Driver: `scripts/digital_lvs.py`
  - Layout under test: `layout/digital/usb_utmi_phy.gds`
  - Reference netlist source: `layout/digital/usb_utmi_phy_routed.v`
  - Raw klt JSON envelopes (frozen):
    `artifacts/20260921-125739-223bae5/lvs-report.json` (the match),
    `artifacts/20260921-125739-223bae5/lvs-report-negctl.json` (the
    negative control),
    `artifacts/20260921-125739-223bae5/extract-report.json`
  - Compared netlists (frozen):
    `artifacts/20260921-125739-223bae5/usb_utmi_phy_layout.spice`,
    `artifacts/20260921-125739-223bae5/usb_utmi_phy_reference.spice`
  - `klt lvs` request (frozen):
    `artifacts/20260921-125739-223bae5/lvs-request.json`
  - Superseded record (kept, append-only):
    `records/20260825-224930-6a83263.md`
  - Manifest that cites this envelope: `manifests/gf180-usb2-phy.json`
    (issue #78)
  - Place-and-route leg that produced this GDS:
    `verification/records/place-and-route/records/20260825-224709-6a83263.md`
  - DRC leg against the same GDS:
    `verification/records/digital-drc/records/20260825-224815-6a83263.md`
- **Timestamp / author**: 2026-09-21T12:57:39Z, loom-builder (issue #78)
- **Supersedes**: 20260825-224930-6a83263 — same claim, same inputs,
  re-run at the re-pinned `klt` (`e8ca621a6961`) because the old envelope
  predates `klt lvs`'s `provenance.input` recording (klayout-tools#1969)
  and cannot carry the `content_hash` pin the T1 signoff manifest's item
  4 requires.
