<!-- record-meta
{
  "record_id": "20260921-132813-1376d98",
  "experiment": "digital-lvs",
  "supersedes": "20260825-224930-6a83263",
  "git_revision": "1376d98ae9fd9ceef14123fc89f4e9570def061d",
  "provenance": {
    "klt_version": "0.5.0+ge8ca621a6961",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637",
      "released": false
    },
    "inputs": [
      { "path": "layout/digital/usb_utmi_phy.gds", "content_hash": "sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476" },
      { "path": "layout/digital/usb_utmi_phy_routed.v", "content_hash": "sha256:c1e0e8ed6e887ed1e45d1371a69e23f23fd3d5344dc73e9c2e963b3d15e6fc87" },
      { "path": "scripts/digital_lvs.py", "content_hash": "sha256:fef15d1f1ec431c1d83fae5537b44559aa85ea7b15ba8cb32134ec7797378310" },
      { "path": "flow/request-usb-utmi-phy-par.json", "content_hash": "sha256:5e267e8645ade958cadc62e93ab523278d0f84c8ef9e8f1a123e1442036e27e2" }
    ]
  }
}
-->

# Record 20260921-132813-1376d98

- **Record ID**: 20260921-132813-1376d98
- **Claim**: re-runs the superseded record's exact claim — **gate-level LVS
  on the digital PHY is a match**, `layout/digital/usb_utmi_phy.gds`
  instantiates exactly the standard cells the as-built gate-level netlist
  `layout/digital/usb_utmi_phy_routed.v` names, wired to exactly the same
  nets, with the same top-level pin set — with every design input
  byte-identical to the superseded run and exactly two things changed: the
  `klt` pin (this repo's re-pin to `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`,
  see `docs/environment-setup.md`'s re-pin history, move 6) and the `klayout`
  engine (0.30.10, the version that pin reports as tested). The result
  reproduces `status: "match"`, and the re-minted envelope records
  `provenance.input.content_hash` — which pre-#1969 `klt lvs` envelopes
  (`20260825-224930-6a83263`'s among them) do not, and which is what makes a
  `klt signoff --manifest` "LVS clean" citation freshness-pinnable at all.
- **Design provenance**: layout (post-P&R, **extracted**) vs. netlist
  (post-P&R as-built, `write_verilog` — CTS buffers, resizes and antenna
  repair included) — unchanged from the superseded record; the extraction
  input hash below is still `sha256:341ccb…`, the committed GDS.
- **Run configuration**: this record is the second in-PR mint of the same
  run (its path-portability defect was caught by this PR's own CI parity
  check before merge — see the PR #81 review thread — and the first mint
  was replaced within the branch, not superseded on `main`). The run:
  ```
  # from a scratch cwd, with the repo's .venv (klt 0.5.0+ge8ca621a6961,
  # klayout 0.30.10) active:
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py --out-dir .
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py --out-dir . --negative-control
  ```
  `--out-dir .` makes the generated request, and so the envelope itself,
  name the extracted layout-side netlist as `./usb_utmi_phy_layout.spice` —
  a path relative to the run's own working directory, mirroring the
  "`klt lvs` echoes `layout`/`reference` exactly as the request document gave
  them" contract and the "evidence committed beside its inputs" shape
  `klt signoff`'s `input_verified` re-hash resolution documents
  (`docs/cli/signoff.md` — the first mint named this worktree's absolute
  scratch path, which only re-hashed to `true` on the machine that produced
  it and rendered `input_verified: null` for every other checkout,
  CI included). The six frozen artifacts below are committed side by side,
  so the envelope's named input resolves and re-hashes against the frozen
  sibling **on any machine that has this repo checked out**.
- **Statistical convention**: N/A — a single deterministic compare of a
  fixed netlist pair, plus one deterministic negative control.
- **Result**:
  - **`status: "match"`, `mismatch_count: 0`, `error_count: 0`.**
  - Counts: nets layout 373 / reference 373 (685 matched); pins 37 / 37
    (349 matched); devices 0 / 0 — by construction (every transistor inside
    an abstracted cell). Inventory reconciled: 352 logic instances across
    55 cell types on both sides; 35 top-level RTL ports plus `VDD`/`VSS`.
  - **Negative control**: the deliberately broken reference
    (`usb_utmi_phy_reference_negctl.spice`, not a frozen artifact — same
    convention as the superseded record) is correctly rejected —
    `status: "mismatch"`, one `category: "topology"` entry. The compare
    measures something at the new pin.
  - **Determinism, stated rather than assumed**: the extracted layout-side
    netlist is byte-identical across three consecutive runs on this change
    (two mint passes plus this one): `environment.layout_sha256` =
    `provenance.input.content_hash` =
    `sha256:c6819988aee1a2ec59cf6fb8e9e03baf9135d589203c7d1c9ebad0040351c2af`
    in every one. The reference generator is likewise deterministic from
    the same Verilog (`sha256:55078e6…`, byte-identical to the superseded
    record's reference).
  - **Overall: pass** — the superseded record's verdict reproduces at the
    re-pinned toolchain.
- **klt provenance**: `klt` 0.5.0+ge8ca621a6961 (git revision
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`), `klayout` 0.30.10
  (`klayout_version_mismatch: false`). Extraction deck `gf180mcu`,
  content_hash
  `sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637`,
  `released: false` — the deck content differs from the superseded
  record's (`sha256:79e71a1e…`, `released: true`) because the re-pin moved
  past the old pin and the shipped deck moved with it; the verdict was
  re-derived against the deck this pin ships, not carried over. PDK
  `gf180mcuD`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`.
- **Links**:
  - Driver: `scripts/digital_lvs.py` (hash byte-identical to the
    superseded record's — the portability fix is in how the run is
    invoked, not in the script)
  - Layout under test: `layout/digital/usb_utmi_phy.gds`
  - Reference netlist source: `layout/digital/usb_utmi_phy_routed.v`
  - Raw klt JSON envelopes (frozen):
    `artifacts/20260921-132813-1376d98/lvs-report.json` (the match),
    `artifacts/20260921-132813-1376d98/lvs-report-negctl.json` (the
    negative control),
    `artifacts/20260921-132813-1376d98/extract-report.json`
  - Compared netlists (frozen beside the envelope that names them):
    `artifacts/20260921-132813-1376d98/usb_utmi_phy_layout.spice`,
    `artifacts/20260921-132813-1376d98/usb_utmi_phy_reference.spice`
  - `klt lvs` request (frozen):
    `artifacts/20260921-132813-1376d98/lvs-request.json`
  - Superseded record (kept, append-only):
    `records/20260825-224930-6a83263.md`
  - Manifest that cites this envelope (items 4 and 11.digital):
    `manifests/gf180-usb2-phy.json`; the verdict of record it renders:
    `manifests/t1-signoff-report.json` (issue #78)
  - Place-and-route leg that produced this GDS:
    `verification/records/place-and-route/records/20260825-224709-6a83263.md`
  - DRC leg against the same GDS:
    `verification/records/digital-drc/records/20260825-224815-6a83263.md`
- **Timestamp / author**: 2026-09-21T13:28:13Z, loom-builder (issue #78,
  Doctor round 1 — re-minted per the review finding on PR #81)
- **Supersedes**: 20260825-224930-6a83263 — same claim against the same
  design inputs; superseded because its `klt` pin aged out (this cycle's
  re-pin) and because its envelope predates `klt lvs`'s
  `provenance.input` recording (klayout-tools#1969), so a
  `content_hash`-pinned "LVS clean" citation against it grades
  `unverifiable_provenance` rather than fresh.
