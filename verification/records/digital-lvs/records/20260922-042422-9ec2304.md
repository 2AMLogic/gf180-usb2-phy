<!-- record-meta
{
  "record_id": "20260922-042422-9ec2304",
  "experiment": "digital-lvs",
  "supersedes": "20260921-145814-5bee855",
  "git_revision": "9ec2304c8aa3cc18fe1c50f3c48fdb38e0997db3",
  "provenance": {
    "klt_version": "0.5.0+ge8ca621a6961",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637",
      "released": false
    },
    "inputs": [
      { "path": "layout/digital/usb_utmi_phy.gds", "content_hash": "sha256:32e6dead081fefe5a237a66196699f6fb3cbcd032061964868104dd7d1ff484f" },
      { "path": "layout/digital/usb_utmi_phy_routed.v", "content_hash": "sha256:188c15b9b06a64be4727174ff420bc2ecafe95f6dbab9508a08ada191e090276" },
      { "path": "scripts/digital_lvs.py", "content_hash": "sha256:8aabec92f580979648e76cecaca3bff92592821d7375228280405b995e3094a4" },
      { "path": "flow/request-usb-utmi-phy-par.json", "content_hash": "sha256:5e267e8645ade958cadc62e93ab523278d0f84c8ef9e8f1a123e1442036e27e2" }
    ]
  }
}
-->

# Record 20260922-042422-9ec2304

- **Record ID**: 20260922-042422-9ec2304
- **Claim**: re-runs the superseded record's exact claim — **gate-level
  LVS on the digital PHY is a full verdict**: `layout/digital/usb_utmi_phy.gds`
  instantiates exactly the standard cells the as-built gate-level netlist
  `layout/digital/usb_utmi_phy_routed.v` names, wired to exactly the same
  nets, with the same top-level pin set, **and** every power/ground pin
  of every instantiated cell lands on the net it should
  (`power_connectivity.status: "match"`) — against the **post-#84
  re-implementation** of both files (issue #87's P&R re-run from the
  vendored `rtl/common/` sources). The script is byte-identical to the
  superseded run's (`scripts/digital_lvs.py` hash `8aabec92…`, unchanged);
  only the two measured artifacts changed.
- **Design provenance**: layout (post-P&R, extracted with cells held as
  black boxes) vs. netlist (post-P&R as-built, `write_verilog` — CTS
  buffers, resizes and antenna repair included) — both committed at
  `9ec2304c8aa3cc18fe1c50f3c48fdb38e0997db3`, both produced by the
  same-day place-and-route record. The compare's reference copy is a
  byte-identical copy of the as-built Verilog (`reference_sha256`
  `188c15b9…` — the same hash as the committed
  `usb_utmi_phy_routed.v`), per the script's own portability
  convention.
- **Run configuration**:
  ```
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py --negative-control
  ```
  (with the repo venv's `klt 0.5.0+ge8ca621a6961` on `$PATH`), `klayout
  0.30.10`, cell library `gf180mcu_fd_sc_mcu9t5v0` (abstracted
  `<library>__*`), `reference.form: "gate-level-verilog"`,
  `options.power_connectivity.expected_nets` naming `VDD`/`VSS` to
  themselves (read from the P&R request's own `power` block). Same
  invocation shape as the superseded record; only the input artifacts
  moved.
- **Statistical convention**: N/A — one deterministic compare against
  one fixed GDS + netlist pair, plus its negative control.
- **Result**:
  - **Signal compare: `status: "match"`, `error_count: 0`.** Net counts:
    349 layout-side / 341 reference-side / 667 matched; pin
    correspondences 361. The four `mismatches[]` entries the report
    carries are the tool's own disclosed *warnings*, the same categories
    and shape the superseded record documented:
    `topology.power_only_pruned` ×1 (the physical-only filler/tap masters
    structurally pruned from the signal compare) and
    `topology.reference_port_alias_joined` ×3 (`LineState[0]`,
    `LineState[1]`, `RxActive` — Yosys `assign`-aliased output ports
    joined to their driving nets, the aliasing the gate-level-Verilog
    reader discloses instead of the old hand-transcription working
    around).
  - **Power/ground half: `power_connectivity.status: "match"`** —
    `VDD`/`VSS` derived as declared by every instantiated master, every
    power pin reaching its expected net.
  - **Negative control: correctly rejected** — the deliberately-broken
    reference (one rewritten connection) reports `status: "mismatch"`
    with 5 mismatches. The clean result above therefore means
    something.
  - **Overall: pass** (script exit 0 — the full-verdict condition).
  - `body_verification.status: "unchecked"` with the tool's own stated
    reason (no `request.layout.deck` on the pre-extracted request form)
    — unchanged in kind from the superseded record; substrate/well-tap
    body conventions are not what this compare claims to check.
- **Coverage gaps** (unchanged from the superseded record): standard
  cells are black boxes (the assembly is verified, not the foundry's
  library); no transistor-level equivalence; no rail/grid continuity in
  the physical sense (IR drop, unbroken rails — `klt power` /
  `klt ring-check` questions).
- **klt provenance**: `klt 0.5.0+ge8ca621a6961`, `klayout 0.30.10`
  (engine self-reported `0.30.10`, no version-mismatch flag), PDK
  `gf180mcuD` (open_pdks `c6d73a35f524…`, `PDK_ROOT=~/.volare`), extract
  deck `gf180mcu` content_hash
  `sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637`.
- **Links**:
  - Layout under test: `layout/digital/usb_utmi_phy.gds`
  - Netlist under test: `layout/digital/usb_utmi_phy_routed.v`
  - Producing P&R record:
    `verification/records/place-and-route/records/20260922-042422-9ec2304.md`
  - Raw artifacts (frozen, netlist paths `./`-normalized per the
    portability convention): `artifacts/20260922-042422-9ec2304/`
    — `lvs-report.json`, `lvs-report-negctl.json`, `lvs-request.json`,
    `extract-report.json`, `usb_utmi_phy_layout.spice`,
    `usb_utmi_phy_reference.v`, `usb_utmi_phy_reference_negctl.v`
  - Friction filed: none.
- **Timestamp / author**: 2026-09-22T04:24:22Z, loom-builder (issue #87)
- **Supersedes**: `20260921-145814-5bee855` — that record's full-verdict
  pass was true of the pre-#84 GDS/netlist pair it pinned
  (`341ccb21…` / `c1e0e8ed…`); #87's re-run replaced both artifacts, so
  its provenance is stale by construction. It stands as history.
