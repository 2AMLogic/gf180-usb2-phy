<!-- record-meta
{
  "record_id": "20260921-145814-5bee855",
  "experiment": "digital-lvs",
  "supersedes": "20260921-132813-1376d98",
  "git_revision": "5bee855056a0b777e26cc548af53a89cc8055d06",
  "provenance": {
    "klt_version": "0.5.0+ge8ca621a6961",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637",
      "released": false
    },
    "inputs": [
      { "path": "layout/digital/usb_utmi_phy.gds", "content_hash": "sha256:341ccb21884ef3808e3483b540f7c68f0caec29f6f8eadb1e63bbf85f1df7476" },
      { "path": "layout/digital/usb_utmi_phy_routed.v", "content_hash": "sha256:c1e0e8ed6e887ed1e45d1371a69e23f23fd3d5344dc73e9c2e963b3d15e6fc87" },
      { "path": "scripts/digital_lvs.py", "content_hash": "sha256:8aabec92f580979648e76cecaca3bff92592821d7375228280405b995e3094a4" },
      { "path": "flow/request-usb-utmi-phy-par.json", "content_hash": "sha256:5e267e8645ade958cadc62e93ab523278d0f84c8ef9e8f1a123e1442036e27e2" }
    ]
  }
}
-->

# Record 20260921-145814-5bee855

- **Record ID**: 20260921-145814-5bee855
- **Claim**: gate-level LVS on the digital PHY is now a **full** verdict —
  the routed GDS implements exactly the signal connectivity the as-built
  gate-level netlist `layout/digital/usb_utmi_phy_routed.v` names **and**
  every power/ground pin of every instantiated `gf180mcu_fd_sc_mcu9t5v0`
  cell reaches its expected net (`VDD`→`VDD`, `VSS`→`VSS`, per
  `flow/request-usb-utmi-phy-par.json`'s own `power` block). The
  superseded record could only substantiate the signal half: its
  `power_connectivity.status` read `"unchecked"` because the
  hand-transcribed plain-element SPICE reference this script used to build
  is not the shape `options.power_connectivity` is honored for — only
  `reference.form: "gate-level-verilog"` is (klayout-tools#1952). This
  record closes that gap: the reference is the as-built Verilog itself,
  so the power/ground half of the report is computed, not `"unchecked"`.
- **Design provenance**: layout (post-P&R, extracted) vs. netlist (post-P&R
  as-built, `write_verilog` — CTS buffers, resizes and antenna repair
  included) — the same signoff pair as the superseded record, byte-identical
  inputs; only the *compare strategy* changed, driven by
  `scripts/digital_lvs.py`.
- **Run configuration**: same pinned environment as the superseded record —
  the repo's `.venv` (klt `0.5.0+ge8ca621a6961`, git revision
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`, this repo's documented pin;
  `klayout` `0.30.10` explicitly held, the `klayout_version_expected` this
  pin reports as tested — a venv left to resolve the newest `klayout`
  carries `klayout_version_mismatch: true` and is discarded, not
  committed). **No further `klt` re-pin was required**: the
  `gate-level-verilog` form and `options.power_connectivity` are both
  already present at the current pin `e8ca621a` (re-pin move 6,
  `docs/environment-setup.md`), so this run happened on exactly the
  documented toolchain. From a scratch cwd with the venv active:
  ```
  # (venv active: klt 0.5.0+ge8ca621a6961, klayout 0.30.10 on PATH)
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py --out-dir .
  PDK_ROOT=~/.volare PDK=gf180mcuD python3 scripts/digital_lvs.py --out-dir . --negative-control
  ```
  `--out-dir .` keeps the request document — and so the envelope — naming
  `./usb_utmi_phy_layout.spice` and `./usb_utmi_phy_reference.v`, the same
  path-portability contract the superseded record's second mint fixed (see
  its "Run configuration"; the reference is now a byte-identical copy of
  the committed routed Verilog rather than a generated transcription, so
  `environment.reference_sha256` re-hashes to the committed
  `layout/digital/usb_utmi_phy_routed.v` exactly). Operational note for
  re-running: the venv must be *active* (klt on `$PATH`) before invoking
  the script — `python3 scripts/digital_lvs.py` under the venv interpreter
  alone still leaves the subprocess `klt` resolved from the ambient
  `$PATH`; on this host that ambient build is v0.5.0-at-tag
  (`6bf5610…`, 224 commits behind the pin, no `power_connectivity`
  support) and yields a `"unchecked"`-shaped envelope from an older
  `klt lvs`. The mint below ran with the venv active; an earlier ambient-
  klt attempt produced exactly that wrong-shape envelope and was
  discarded, not committed.
- **Statistical convention**: N/A — a single deterministic compare of a
  fixed netlist pair, plus one deterministic negative control.
- **Result**:
  - **Signal half: `status: "match"`, `error_count: 0`, `mismatch_count:
    4` — all four `severity: "warning"` disclosures, zero errors.** The
    four entries are the tool's own names for the flow-level workarounds
    this request form makes explicit rather than hand-managed:
    one `topology.power_only_pruned` (the 9 physical-only masters
    `ENDCAP`, `FILLTIE`, `FILL_1/2/4/8/16/32/64` — exactly the classes
    the superseded run's `PHYSICAL_ONLY` filter used to exclude by name —
    pruned structurally by `klt lvs`, not by a script-side glob) and
    three `topology.reference_port_alias_joined` (`LineState[0]`→
    `line_state[0]`, `LineState[1]`→`line_state[1]`,
    `RxActive`→`rx_receiving` — the `assign`-alias joins the superseded
    run's `parse_verilog` used to resolve by hand). Counts: nets 373
    layout / 368 reference (682 matched), pins 37 / 35 (349 matched),
    devices 0/0 — 352 logic instances across 55 cell types on both
    sides, plus 854 filler/tie instances the layout carries and the
    signal-only reference correctly does not.
  - **Power/ground half: `power_connectivity.status: "match"`,
    `finding_count: 0`, `unchecked_expected_pins: []`.** The check
    covered all 1206 abstracted instances (352 logic + 854
    fill/tap/endcap — power-only cells are checked *before* the signal
    compare's prune, which is the point: their power connectivity is the
    only thing about them any check could verify). The pin universe
    `["VDD", "VSS"]` was derived by the supported rule
    (`declared-by-every-instantiated-master`, 55 masters,
    `corroborated: true`); `expected_nets` declared
    `{"VDD": "VDD", "VSS": "VSS"}` — the design's own
    `power.power_net`/`ground_net` read from the P&R request — so the
    verdict is absolute, not merely cross-instance agreement. The
    library's well-tie pins (`VNW`/`VPW`, declared by the
    `gf180mcu_fd_sc_mcu9t5v0` masters) did **not** enter the checked
    universe this run: the derived layout-side pin set carried only
    `VDD`/`VSS` (black-box pin resolution found in-cell `VDD`/`VSS`
    labels only), so no well-tie finding could fire either way — a
    coverage limit of the abstraction's pin resolution on this deck, not
    a green claim about well ties.
  - **Negative control**: the deliberately broken reference (the first
    standard-cell instance's first named connection rewired
    `inv_1 _146_: .I(rx_receiving) -> .I(_009_)` in a scratch copy,
    `usb_utmi_phy_reference_negctl.v`, not a frozen artifact — the
    Verilog-form replacing of the superseded record's SPICE-transcription
    break) is correctly rejected: `status: "mismatch"`,
    `error_count: 1` (a `severity: "error"`
    `topology` "subcircuit instance could not be matched to a
    counterpart" entry). The compare measures something at this request
    form.
  - **Determinism, stated rather than assumed**: the extracted layout-side
    netlist is byte-identical across every extraction pass of this issue
    (worktree debugging passes on klayout 0.30.12 — discarded for the
    envelope's `klayout_version_mismatch: true`, see below — plus the
    klayout 0.30.10 worktree passes plus both of this record's mint
    passes): `environment.layout_sha256` =
    `provenance.input.content_hash` =
    `sha256:89f86b12ecec60b92e6d0b28084e40e57d8cf5814994140424034a33ac51002c`
    in every one. The reference side is deterministic by construction: a
    byte-identical copy of the committed routed Verilog
    (`sha256:c1e0e8ed…`, equal to `environment.reference_sha256`). Note
    the extracted-netlist hash differs from the superseded record's
    (`sha256:c68199…`) **by design**: this run abstracts every
    `gf180mcu_fd_sc_mcu9t5v0__*` cell, fillers/taps included (the
    documented wildcard form), where the superseded run flattened the
    physical-only types — so the extracted netlist now carries 854
    black-box filler instances the old extraction did not.
  - **Overall: pass** — the first gate-level LVS verdict on this block
    that covers the power/ground half.
- **klt provenance**: `klt` 0.5.0+ge8ca621a6961 (git revision
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`, unchanged from re-pin move 6
  — this record required **no** pin move), `klayout` 0.30.10
  (`klayout_version_mismatch: false` — held explicitly in the venv per
  `docs/environment-setup.md`'s engine note). Extraction deck `gf180mcu`,
  content_hash
  `sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637`,
  `released: false` — identical to the superseded record's deck hash (the
  pin did not move between the two records). PDK `gf180mcuD`, open_pdks
  `c6d73a35f524070e85faff4a6a9eef49553ebc2b`.
- **Links**:
  - Driver: `scripts/digital_lvs.py` (the harness whose reference
    strategy this change flipped: `gate-level-verilog` form, wildcard
    `--abstract-cells`, `expected_nets` from the P&R request's power
    block; the hand-rolled SPICE transcription is deleted)
  - Layout under test: `layout/digital/usb_utmi_phy.gds`
  - Reference netlist source: `layout/digital/usb_utmi_phy_routed.v`
  - Raw klt JSON envelopes (frozen):
    `artifacts/20260921-145814-5bee855/lvs-report.json` (the full pass),
    `artifacts/20260921-145814-5bee855/lvs-report-negctl.json` (the
    negative control),
    `artifacts/20260921-145814-5bee855/extract-report.json`
  - Compared netlists (frozen beside the envelope that names them):
    `artifacts/20260921-145814-5bee855/usb_utmi_phy_layout.spice`,
    `artifacts/20260921-145814-5bee855/usb_utmi_phy_reference.v`
  - `klt lvs` request (frozen):
    `artifacts/20260921-145814-5bee855/lvs-request.json`
  - Superseded record (kept, append-only):
    `records/20260921-132813-1376d98.md`
  - Manifest whose item 4 / item 11.digital citations this envelope now
    backs (re-pointed in the same change, per
    `scripts/check_signoff_report.py`'s one-change rule):
    `manifests/gf180-usb2-phy.json`; the frozen render:
    `manifests/t1-signoff-report.json`
  - Place-and-route leg that produced this GDS:
    `verification/records/place-and-route/records/20260825-224709-6a83263.md`
  - DRC leg against the same GDS:
    `verification/records/digital-drc/records/20260825-224815-6a83263.md`
- **Timestamp / author**: 2026-09-21T14:58:14Z, loom-builder (issue #79)
- **Supersedes**: 20260921-132813-1376d98 — same claim's signal half against
  the same byte-identical design inputs; superseded because its
  plain-element SPICE reference left `power_connectivity.status` reading
  `"unchecked"` (the form `options.power_connectivity` is not honored for),
  which is precisely the gap T1 item 11's digital leg was filed to close
  (issue #79). **Closing this issue does NOT flip item 11's digital
  partition to `met`**: the item has a second, independent blocker — the
  `digital-erc` record's deliberate `ties[]` omission
  (`layout/digital/erc-supply-spec.json`, klayout-tools#2169's
  well-conduction collapse bug, fixed 2026-09-20 by PR #2186 but not yet
  re-derived against) — so `klt signoff --manifest` keeps grading item 11's
  digital partition `unmet` with reason `supply_spec_incomplete`, and a
  reader of the rendered report is not misled by this record's existence.
