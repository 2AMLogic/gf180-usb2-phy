<!-- record-meta
{
  "record_id": "20260818-025302-ea10f21",
  "experiment": "utmi-framing-functional",
  "supersedes": "20260817-184228-72de176",
  "git_revision": "ea10f21bbb041665f93a0f81e2fc116ea8fb10e3",
  "provenance": {
    "klt_version": "0.2.0",
    "pdk": {
      "name": "n/a",
      "version": "n/a"
    },
    "deck": {
      "content_hash": "n/a"
    },
    "inputs": [
      {
        "path": "rtl/usb_line_state_decode.v",
        "content_hash": "sha256:c2d93d43d48ddaa2b620c3c0f11b06af74e89860fa3883f7b543955b46aa0d08"
      },
      {
        "path": "rtl/usb_sync_detector.v",
        "content_hash": "sha256:1f69da425fec809895fbdeb61e1d308379ddabc9aa0ecb10bf2f88f8fa88cbdc"
      },
      {
        "path": "rtl/usb_eop_detector.v",
        "content_hash": "sha256:163b950b3dcf939a2810f03b63dbea6f361f3261d47a75ea980cdf60d67e8e28"
      },
      {
        "path": "rtl/usb_utmi_phy.v",
        "content_hash": "sha256:9f0a9c95056934a0a8a7e357ae0b8c44b0e5b6704e94e6b433c478e4ecfb870b"
      },
      {
        "path": "rtl/usb_bit_stuffer.v",
        "content_hash": "sha256:1f53016e105414fed26e560a64e7d0db7995d32238c3ac3d5070a0c5bf6c8543"
      },
      {
        "path": "rtl/usb_nrzi_encoder.v",
        "content_hash": "sha256:3f177079cc4112d34d9c9e5864b406481a6735a3ab7a840ccd28f991fc80f5d7"
      },
      {
        "path": "rtl/usb_nrzi_decoder.v",
        "content_hash": "sha256:ac31765a4389a8502526843cdca2edffd6be8b7f4bb4f2fcb3061b5ae85c384b"
      },
      {
        "path": "rtl/usb_bit_destuffer.v",
        "content_hash": "sha256:2c16654e2f699f6b8692da99563da50bf186f41c89a107c6e2fa8b27f6c6d760"
      },
      {
        "path": "verification/test_usb_line_state_decode.py",
        "content_hash": "sha256:b4a4f134f656ed2a628dd7c94f3768f8306e605a42f061419e56c5c36efe1792"
      },
      {
        "path": "verification/test_usb_sync_detector.py",
        "content_hash": "sha256:386c85b1b4a3c263dd1ad2bf3e4c7e20b705b67e7a2a002fab268f3df0214fbe"
      },
      {
        "path": "verification/test_usb_eop_detector.py",
        "content_hash": "sha256:aefa95afcf97bef15cac92fa79d3ecda73fff067773cbcc15eabf1bd73fdf9ce"
      },
      {
        "path": "verification/test_usb_utmi_phy.py",
        "content_hash": "sha256:bfe8edf2e27e59adb673f1d5adcffac6ad3772718548d1c4edc3347b6603eab1"
      },
      {
        "path": "verification/usb_bit_model.py",
        "content_hash": "sha256:0467fc82c9b6ca60f584e251c9e4729b3e6c2959d8209d19b1e99b5cb9d50637"
      }
    ]
  }
}
-->

# Record 20260818-025302-ea10f21

- **Record ID**: 20260818-025302-ea10f21
- **Claim**: identical to `20260817-184228-72de176`'s claim -- `spec/usb2-device-phy.md`
  §11's digital signoff line for the **rest** of §2/§4's digital logic
  beyond the bit-level codec: SYNC field detection and RX bit-lock, EOP
  (SE0) detection with the 2-bit-time-EOP-vs-2.5µs-reset disambiguation §4
  requires, `LineState[1:0]` (J/K/SE0/SE1) decode, and SYNC/EOP generation
  (TX); and the top-level `usb_utmi_phy` wrapper exercised end to end. This
  record exists only because issue #46's dead-code removal (below) changed
  the content hash of `verification/test_usb_utmi_phy.py`, which
  `20260817-184228-72de176` pinned by hash -- `verification/check_records.py`'s
  provenance-freshness check correctly flags that as stale, not because the
  claim itself needed re-verifying for any functional reason.
- **Design provenance**: rtl (`rtl/usb_line_state_decode.v`,
  `rtl/usb_sync_detector.v`, `rtl/usb_eop_detector.v`, `rtl/usb_utmi_phy.v`,
  `rtl/usb_bit_stuffer.v`, `rtl/usb_nrzi_encoder.v`, `rtl/usb_nrzi_decoder.v`,
  `rtl/usb_bit_destuffer.v`, all byte-identical to `20260817-184228-72de176`,
  now @ `ea10f21bbb041665f93a0f81e2fc116ea8fb10e3`). `verification/usb_bit_model.py`
  and three of the four testbenches (`test_usb_line_state_decode.py`,
  `test_usb_sync_detector.py`, `test_usb_eop_detector.py`) are likewise
  byte-identical to the superseded record -- see the unchanged
  `content_hash` values above. The only input that changed is
  `verification/test_usb_utmi_phy.py`: issue #46 deleted `_bits_to_byte()`,
  a 5-line helper (plus its two separating blank lines) that was the unused
  inverse of `_byte_to_bits()` -- confirmed to have zero call sites anywhere
  in the repo both before and after removal. No RTL changed, no test logic,
  sequencing, or assertion changed -- only a dead helper function removed.
- **Run configuration**: identical to `20260817-184228-72de176` -- `klt
  functional-verification` (engine `icarus`), four separate invocations
  because the request schema names a single `hdl_toplevel`:
  `verification/request-usb-line-state-decode.json`
  (`usb_line_state_decode` / `test_usb_line_state_decode`),
  `verification/request-usb-sync-detector.json`
  (`usb_sync_detector` / `test_usb_sync_detector`),
  `verification/request-usb-eop-detector.json`
  (`usb_eop_detector` / `test_usb_eop_detector`), and
  `verification/request-usb-utmi-phy.json`
  (`usb_utmi_phy` / `test_usb_utmi_phy`, which elaborates all eight RTL
  sources above in one run). Every request uses `options.random_seed` = 1,
  `options.timescale` = `["1ns", "1ps"]`, `options.coverage` = false, and
  no `testcase` filter. Every testbench still clocks its DUT at 83334 ps
  (12 MHz to within 0.001%). Icarus Verilog 12.0, cocotb 2.0.1, klt 0.2.0.
- **Statistical convention**: unchanged from `20260817-184228-72de176` --
  every case in this record is a directed, deterministic check (exact SYNC
  pattern, exact EOP/reset boundary clocks, exact TX SYNC/EOP wire shapes, a
  fixed set of TX byte payloads for the loopback round trips) rather than a
  randomized cross-check -- N/A for a sample-size/seed beyond the fixed
  `options.random_seed` = 1 every request carries.
- **Result**:
  - `usb_line_state_decode`: 6/6 PASS (2083.350 ns total sim time)
  - `usb_sync_detector`: 7/7 PASS (11541.759 ns)
  - `usb_eop_detector`: 6/6 PASS (9666.744 ns)
  - `usb_utmi_phy`: 8/8 PASS (48500.388 ns)
  - **Totals: 27 tests, 27 passed, 0 failed, 0 skipped. Overall: PASS.**
  - Every per-test `sim_time_ns` in this run's artifacts is bit-for-bit
    identical to `20260817-184228-72de176`'s -- expected, since the DUTs,
    stimulus, seeds, and clock period are unchanged; only an unused helper
    function was deleted from the testbench file. This is exactly the
    outcome a behavior-preserving cleanup should produce and is independent
    corroboration that issue #46's removal changed nothing observable.
  - All edge cases remain covered as documented in `20260817-184228-72de176`
    (all four `LineState[1:0]` encodings, the exact SYNC pattern and its
    negative cases, both SE0-duration boundaries and their
    EOP-vs-bus-reset disambiguation, TX SYNC/EOP wire shapes, multi-byte
    loopback with bit stuffing, the USB 2.0 §7.1.9 trailing-stuff-bit
    flush case, back-to-back packets with minimal gap, and a malformed
    SYNC that never activates RX).
- **klt provenance**: klt 0.2.0; PDK: n/a (an Icarus/cocotb functional
  verification of standard-cell-library-agnostic RTL has no PDK
  dependency), so `deck.content_hash` is n/a as well; input content_hashes
  for all eight RTL sources, the four testbenches, and the shared
  `usb_bit_model.py` reference model are listed under `provenance.inputs`
  in the metadata block above. This run used a pip-installed
  `klayout-tools`/`cocotb==2.0.1` environment matching
  `.github/workflows/ci.yml`'s pin (Icarus Verilog 12.0, present natively
  on this host's `$PATH`).
- **Links**:
  - Request recipes: `verification/request-usb-line-state-decode.json`,
    `verification/request-usb-sync-detector.json`,
    `verification/request-usb-eop-detector.json`,
    `verification/request-usb-utmi-phy.json`
  - Testbenches: `verification/test_usb_line_state_decode.py`,
    `verification/test_usb_sync_detector.py`,
    `verification/test_usb_eop_detector.py`,
    `verification/test_usb_utmi_phy.py`
  - Reference model: `verification/usb_bit_model.py`
  - Designs under test: `rtl/usb_line_state_decode.v`,
    `rtl/usb_sync_detector.v`, `rtl/usb_eop_detector.v`,
    `rtl/usb_utmi_phy.v` (which additionally instantiates
    `rtl/usb_bit_stuffer.v`, `rtl/usb_nrzi_encoder.v`,
    `rtl/usb_nrzi_decoder.v`, `rtl/usb_bit_destuffer.v`)
  - Raw klt JSON envelopes, one per invocation, under
    `verification/records/utmi-framing-functional/artifacts/20260818-025302-ea10f21/`:
    `usb-line-state-decode-output.json`, `usb-sync-detector-output.json`,
    `usb-eop-detector-output.json`, `usb-utmi-phy-output.json`
  - Each envelope's `environment.results_xml` field names the absolute path
    to cocotb's raw `results_icarus.xml` on the host that produced it -- a
    `.klt/functional-verification/` build artifact, gitignored and
    reproduced fresh by any re-run, not committed separately since the
    envelopes already carry every field this record's claim depends on.
- **Timestamp / author**: 2026-08-18T02:53:02Z, loom-builder (issue #46)
- **Supersedes**: `20260817-184228-72de176`. That record's claim still
  holds and its own RTL/model/harness inputs are byte-identical to this
  one's; it is superseded only because one of its `provenance.inputs`
  (`verification/test_usb_utmi_phy.py`) changed content when issue #46
  removed the unused `_bits_to_byte()` helper, which
  `verification/check_records.py`'s provenance-freshness check correctly
  treats as stale. This record re-runs the identical suite against the
  cleaned-up testbench and confirms bit-for-bit identical results (same
  pass/fail counts, same per-test `sim_time_ns` for every one of the 27
  tests), so the underlying claim was never in question -- only the
  citation needed refreshing.
