<!-- record-meta
{
  "record_id": "20260810-205021-c819c95",
  "experiment": "functional-smoke",
  "supersedes": null,
  "git_revision": "c819c95aad440213f5869f1552f308c530c37be7",
  "provenance": {
    "klt_version": "0.2.0",
    "pdk": {
      "name": "n/a",
      "version": "n/a"
    },
    "deck": {
      "content_hash": "n/a"
    },
    "inputs": [
      {
        "path": "rtl/harness_counter.v",
        "content_hash": "sha256:fb4559ab74baf24b26fb27463ac30f57efe00a89e9108e4e86e108a9398c0025"
      },
      {
        "path": "verification/test_harness_counter.py",
        "content_hash": "sha256:ab281fa664002ded875dd9615f86cce3f6f5792f104c3edfa93e16bfe07b4a1f"
      }
    ]
  }
}
-->

# Record 20260810-205021-c819c95

- **Record ID**: 20260810-205021-c819c95
- **Claim**: `verification/README.md#worked-example` -- the digital cocotb +
  Icarus + klt harness bootstrapped by issue #7 elaborates and verifies a
  real (if trivial) design end-to-end: `harness_counter.v`'s three cocotb
  tests all pass via `klt functional-verification`.
- **Design provenance**: rtl (`rtl/harness_counter.v` @
  `c819c95aad440213f5869f1552f308c530c37be7`)
- **Run configuration**: `klt functional-verification` (engine `icarus`),
  `verification/request-harness-counter.json` (`hdl_toplevel` =
  `harness_counter`, testbench module `test_harness_counter`, no
  `testcase` filter -- all 3 tests run), `options.random_seed` = 1,
  `options.timescale` = `["1ns", "1ps"]`.
- **Statistical convention**: `test_counts_and_wraps` draws 500 pseudo-random
  enable decisions from `random.Random(0)` (a fixed seed, reproducible);
  the other two tests are directed, deterministic checks. N/A for an
  overall sample size beyond that.
- **Result**:
  - `test_count`: 3, `passed_count`: 3, `failed_count`: 0
  - `test_reset_holds_zero`: PASS (45.0 ns sim time)
  - `test_disabled_holds_value`: PASS (115.0 ns sim time)
  - `test_counts_and_wraps`: PASS (5025.0 ns sim time; 500/500 random cases
    bit-exact against the Python reference model, including wraparound at
    `2**WIDTH`)
  - **Overall: PASS**
  - Independently reproduced on a second host (Linux, Icarus Verilog 12.0
    vs. the 13.0 originally used) with byte-identical `sim_time_ns` per test
    and an identical pass/fail breakdown -- only `environment.engine_version`
    and the absolute `results_xml` scratch path differed, as expected for a
    host-local, gitignored build directory.
- **klt provenance**: klt 0.2.0; PDK: n/a (Icarus/cocotb functional
  verification of a standard-cell-library-agnostic RTL design has no PDK
  dependency); input content_hashes: `rtl/harness_counter.v` =
  `sha256:fb4559ab74baf24b26fb27463ac30f57efe00a89e9108e4e86e108a9398c0025`,
  `verification/test_harness_counter.py` =
  `sha256:ab281fa664002ded875dd9615f86cce3f6f5792f104c3edfa93e16bfe07b4a1f`
  (see `provenance.inputs` in the metadata block above).
- **Links**:
  - Request recipe: `verification/request-harness-counter.json`
  - Testbench: `verification/test_harness_counter.py`
  - Design under test: `rtl/harness_counter.v`
  - Raw klt JSON envelope (the full per-test pass/fail/timing breakdown,
    klt's own reported source of truth):
    `verification/records/functional-smoke/artifacts/20260810-205021-c819c95/functional-verification-output.json`
  - The envelope's own `environment.results_xml` field names the absolute
    path to cocotb's raw `results_icarus.xml` on the host that produced it
    -- a `.klt/functional-verification/` build artifact, gitignored and
    reproduced fresh by any re-run (see `.gitignore`'s "klt / cocotb /
    Icarus scratch" entry), not committed here separately since the JSON
    envelope already carries every field this record's claim depends on.
- **Timestamp / author**: 2026-08-10T20:50:21Z, loom-builder (issue #7)
- **Supersedes**: none
