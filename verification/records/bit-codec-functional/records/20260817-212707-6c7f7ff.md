<!-- record-meta
{
  "record_id": "20260817-212707-6c7f7ff",
  "experiment": "bit-codec-functional",
  "supersedes": "20260816-074908-4e92fcc",
  "git_revision": "6c7f7ff5282eecfc02b88d75d6f9cef925bcde2b",
  "provenance": {
    "klt_version": "0.2.0",
    "pdk": {
      "name": "n/a",
      "version": "n/a"
    },
    "deck": {
      "content_hash": "n/a"
    },
    "inputs": [
      {
        "path": "rtl/usb_nrzi_encoder.v",
        "content_hash": "sha256:3f177079cc4112d34d9c9e5864b406481a6735a3ab7a840ccd28f991fc80f5d7"
      },
      {
        "path": "rtl/usb_nrzi_decoder.v",
        "content_hash": "sha256:ac31765a4389a8502526843cdca2edffd6be8b7f4bb4f2fcb3061b5ae85c384b"
      },
      {
        "path": "rtl/usb_bit_stuffer.v",
        "content_hash": "sha256:1f53016e105414fed26e560a64e7d0db7995d32238c3ac3d5070a0c5bf6c8543"
      },
      {
        "path": "rtl/usb_bit_destuffer.v",
        "content_hash": "sha256:2c16654e2f699f6b8692da99563da50bf186f41c89a107c6e2fa8b27f6c6d760"
      },
      {
        "path": "verification/tb_usb_bit_codec_loopback.v",
        "content_hash": "sha256:a418320b18a45e7f90a948d839d3bed09144cf7fce6454f9fb35a3fb824a2e37"
      },
      {
        "path": "verification/usb_bit_model.py",
        "content_hash": "sha256:0467fc82c9b6ca60f584e251c9e4729b3e6c2959d8209d19b1e99b5cb9d50637"
      },
      {
        "path": "verification/cocotb_helpers.py",
        "content_hash": "sha256:7ea44aef04b40d34748eb4e4db9502d4d0f5203437d95debb1faadb2ecc85b78"
      },
      {
        "path": "verification/test_usb_nrzi_encoder.py",
        "content_hash": "sha256:349d4adb229675841c7f8722d71c386c3220f5464bae5d93becb9d28a8604b4e"
      },
      {
        "path": "verification/test_usb_nrzi_decoder.py",
        "content_hash": "sha256:fe0e780039f3a86eeed02f23809b5378d896033a16fe2cce5b5b72ab905404fe"
      },
      {
        "path": "verification/test_usb_bit_stuffer.py",
        "content_hash": "sha256:e3a4b3ebf1304b035b7cd5a9b36accef9fe6dcce68834bd289e0e2349a3fac49"
      },
      {
        "path": "verification/test_usb_bit_destuffer.py",
        "content_hash": "sha256:5dc463d172eb8af5a796ad45c520f45ae579dee314ac70aaa6151b9f6858a70a"
      },
      {
        "path": "verification/test_usb_bit_codec_loopback.py",
        "content_hash": "sha256:cb3df10cfa2c6fe18da041c2b3ac8cfa48382a6aff5f7fc27d090079e5ca2e36"
      }
    ]
  }
}
-->

# Record 20260817-212707-6c7f7ff

- **Record ID**: 20260817-212707-6c7f7ff
- **Claim**: identical to `20260816-074908-4e92fcc`'s claim -- `spec/usb2-device-phy.md`
  §11's digital signoff line for the **bit-level half** of §2 (NRZI encode
  TX, NRZI decode RX, bit stuffing TX including the USB 2.0 §7.1.9 trailing
  stuff bit, bit destuffing RX including a bit-stuff error on seven
  consecutive 1s). §2's SYNC/EOP and `LineState[1:0]` clauses are still not
  claimed here. This record exists only because issue #44's testbench
  refactor (below) changed the content of the five testbench files that
  `20260816-074908-4e92fcc` pinned by hash, which the record-freshness check
  in `verification/check_records.py` correctly flags as staleness -- not
  because the claim itself needed re-verifying for any functional reason.
- **Design provenance**: rtl (`rtl/usb_nrzi_encoder.v`,
  `rtl/usb_nrzi_decoder.v`, `rtl/usb_bit_stuffer.v`,
  `rtl/usb_bit_destuffer.v`, all byte-identical to
  `20260816-074908-4e92fcc`, now @ `6c7f7ff5282eecfc02b88d75d6f9cef925bcde2b`).
  `verification/tb_usb_bit_codec_loopback.v` and
  `verification/usb_bit_model.py` are likewise byte-identical to the
  superseded record -- see the unchanged `content_hash` values above. The
  only inputs that changed are the testbenches themselves: issue #44
  extracted the byte-identical `CLK_PERIOD_PS` constant and
  `_start_clock(dut)` helper -- previously redefined independently in all
  four per-module testbenches plus the loopback testbench -- into a new
  shared module, `verification/cocotb_helpers.py`
  (`start_clock(dut)`/`CLK_PERIOD_PS`), imported as
  `from cocotb_helpers import start_clock as _start_clock`. `_reset(dut)`
  was deliberately left alone in every file (it drives different DUT signal
  names per module, so parameterizing it was judged not worth the
  indirection -- see `cocotb_helpers.py`'s own docstring). No RTL changed,
  no test logic, sequencing, or assertion changed -- only where the clock
  constant and its start coroutine are defined.
- **Run configuration**: identical to `20260816-074908-4e92fcc` --
  `klt functional-verification` (engine `icarus`), five separate
  invocations because the request schema names a single `hdl_toplevel`:
  `verification/request-usb-nrzi-encoder.json` (`usb_nrzi_encoder` /
  `test_usb_nrzi_encoder`),
  `verification/request-usb-nrzi-decoder.json` (`usb_nrzi_decoder` /
  `test_usb_nrzi_decoder`),
  `verification/request-usb-bit-stuffer.json` (`usb_bit_stuffer` /
  `test_usb_bit_stuffer`),
  `verification/request-usb-bit-destuffer.json` (`usb_bit_destuffer` /
  `test_usb_bit_destuffer`), and
  `verification/request-usb-bit-codec-loopback.json`
  (`tb_usb_bit_codec_loopback` / `test_usb_bit_codec_loopback`). Every
  request uses `options.random_seed` = 1, `options.timescale` =
  `["1ns", "1ps"]`, `options.coverage` = false, and no `testcase` filter.
  Every testbench still clocks its DUT at 83334 ps (12 MHz to within
  0.001%) via the now-shared `cocotb_helpers.start_clock`. Icarus Verilog
  13.0, cocotb 2.0.1, klt 0.2.0 -- same tool versions as the superseded
  record.
- **Statistical convention**: unchanged from `20260816-074908-4e92fcc` --
  the randomized cross-checks draw from the same fixed, per-testbench
  `random.Random` seeds: `test_usb_nrzi_encoder.test_random_stream_matches_model`
  (seed 20260816, 512 uniform bits),
  `test_usb_nrzi_decoder.test_random_stream_round_trips` (seed 20260817,
  512 uniform bits), `test_usb_bit_stuffer.test_random_stream_matches_model`
  (seed 20260818, 512 bits drawn 80% 1s), `test_usb_bit_destuffer.test_random_stream_round_trips`
  (seed 20260819, 512 bits, same 80% bias), and
  `test_usb_bit_codec_loopback.test_round_trip_random_stream` (seed
  20260820, 512 bits, same 80% bias). Every other test is a directed,
  deterministic check; N/A for an overall sample size beyond the above.
- **Result**:
  - `usb_nrzi_encoder`: 6/6 PASS (50250.402 ns total sim time)
  - `usb_nrzi_decoder`: 6/6 PASS (50583.738 ns)
  - `usb_bit_stuffer`: 9/9 PASS (65875.527 ns)
  - `usb_bit_destuffer`: 9/9 PASS (57292.125 ns)
  - `tb_usb_bit_codec_loopback`: 5/5 PASS (64042.179 ns)
  - **Totals: 35 tests, 35 passed, 0 failed, 0 skipped. Overall: PASS.**
  - Every per-test `sim_time_ns` in this run's artifacts is bit-for-bit
    identical to `20260816-074908-4e92fcc`'s -- expected, since the DUTs,
    stimulus, seeds, and clock period are unchanged; only where the clock
    helper is *defined* moved. This is exactly the outcome a
    behavior-preserving refactor should produce and is independent
    corroboration that issue #44's extraction changed nothing observable.
  - All edge cases and the USB 2.0 §7.1.9 packet-boundary case remain
    covered as documented in `20260816-074908-4e92fcc` (all-1s/all-0s
    density extremes, stuff-error injection, transition-free NRZI decode,
    the flushed trailing-stuff-bit case in all three places it is
    observable, and the loopback leg's seven-bit-time line-hold bound).
- **klt provenance**: klt 0.2.0; PDK: n/a (Icarus/cocotb functional
  verification of standard-cell-library-agnostic RTL has no PDK
  dependency), so `deck.content_hash` is n/a as well; input content_hashes
  for all four RTL sources, the loopback harness, the reference model, the
  new shared `cocotb_helpers.py`, and the five testbenches are listed under
  `provenance.inputs` in the metadata block above. This run used a
  pip-installed `klayout-tools`/`cocotb==2.0.1` environment matching
  `.github/workflows/ci.yml`'s pin exactly (rather than the contributor's
  local pipx-managed `klt` install, which on this host currently hits
  [`2AMLogic/klayout-tools#1103`](https://github.com/2AMLogic/klayout-tools/issues/1103)
  -- an interpreter/wheel ABI mismatch between the pipx venv's Python and
  the system `vvp`, unrelated to this repo's code and already filed against
  the tool).
- **Links**:
  - Request recipes: `verification/request-usb-nrzi-encoder.json`,
    `verification/request-usb-nrzi-decoder.json`,
    `verification/request-usb-bit-stuffer.json`,
    `verification/request-usb-bit-destuffer.json`,
    `verification/request-usb-bit-codec-loopback.json`
  - Testbenches: `verification/test_usb_nrzi_encoder.py`,
    `verification/test_usb_nrzi_decoder.py`,
    `verification/test_usb_bit_stuffer.py`,
    `verification/test_usb_bit_destuffer.py`,
    `verification/test_usb_bit_codec_loopback.py`
  - Shared testbench helpers (new): `verification/cocotb_helpers.py`
  - Reference model: `verification/usb_bit_model.py`
  - Round-trip harness: `verification/tb_usb_bit_codec_loopback.v`
  - Designs under test: `rtl/usb_nrzi_encoder.v`, `rtl/usb_nrzi_decoder.v`,
    `rtl/usb_bit_stuffer.v`, `rtl/usb_bit_destuffer.v`
  - Raw klt JSON envelopes, one per invocation, under
    `verification/records/bit-codec-functional/artifacts/20260817-212707-6c7f7ff/`:
    `usb-nrzi-encoder-output.json`, `usb-nrzi-decoder-output.json`,
    `usb-bit-stuffer-output.json`, `usb-bit-destuffer-output.json`,
    `usb-bit-codec-loopback-output.json`
  - Each envelope's `environment.results_xml` field names the absolute path
    to cocotb's raw `results_icarus.xml` on the host that produced it -- a
    `.klt/functional-verification/` build artifact, gitignored and
    reproduced fresh by any re-run, not committed separately since the
    envelopes already carry every field this record's claim depends on.
- **Timestamp / author**: 2026-08-17T21:27:07Z, loom-builder (issue #44)
- **Supersedes**: `20260816-074908-4e92fcc`. That record's claim still
  holds and its own RTL/model/harness inputs are byte-identical to this
  one's; it is superseded only because five of its `provenance.inputs`
  (the four per-module testbenches plus the loopback testbench) changed
  content when issue #44 extracted their shared `CLK_PERIOD_PS`/`_start_clock`
  boilerplate into `verification/cocotb_helpers.py`, which
  `verification/check_records.py`'s provenance-freshness check correctly
  treats as stale. This record re-runs the identical suite against the
  refactored testbenches and confirms bit-for-bit identical results (same
  pass/fail counts, same per-test `sim_time_ns` for every one of the 35
  tests), so the underlying claim was never in question -- only the
  citation needed refreshing.
