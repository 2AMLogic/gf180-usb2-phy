<!-- record-meta
{
  "record_id": "20260909-213500-29c5780",
  "experiment": "analog-layout",
  "supersedes": "20260906-004835-90e442a",
  "git_revision": "29c5780d1df0a16bb1d14c4af0bbc9ff0ad99b43",
  "provenance": {
    "klt_version": "0.4.0+g8bd3f0b09a87",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:79e71a1e7d84be3cfc82e4c70afbdf7b743ac1f361fb8e981f57831014d2e8b0"
    },
    "inputs": [
      { "path": "scripts/setup-env.sh", "content_hash": "sha256:ea6bd105372a9cabd2657144434368c76ebafbe47d8c78ded3841227385cecaf" },
      { "path": "scripts/gen_analog_layout.py", "content_hash": "sha256:65b7c20e3f852fabadfd4e69a40d9f9794dc18a32c488b42540af5e973abd5d9" },
      { "path": "layout/analog/plans/differential_receiver.json", "content_hash": "sha256:f04c6d416066179975458c7c253db9f6143b3f0e1a98cb4519692a9e5871de1c" },
      { "path": "layout/analog/plans/dplus_pullup.json", "content_hash": "sha256:8d66acb201f3c446e5f27fda9fb096b4da760b8a5aef30d54a6c0714f8168782" },
      { "path": "layout/analog/plans/se_receiver_dm.json", "content_hash": "sha256:4f8d5b08e4007e851fa1095528b40bb926c4fe44864af8cb886472335b810b36" },
      { "path": "layout/analog/plans/se_receiver_dp.json", "content_hash": "sha256:4d16672ea830a0d695ee62edab0b713046854aedc873f9ba67c57152869cec8b" },
      { "path": "design/dplus_pullup.sch", "content_hash": "sha256:81eaf5101b343bc5773f7d2800ea3dec40f8d7a5388240b8d22fd13a0b7684e6" },
      { "path": "design/netlist/differential_driver.spice", "content_hash": "sha256:bd4b2608c986fd1e305be7b1522e94f578cf36206fe30c13325f48a55f46afa5" },
      { "path": "design/netlist/differential_receiver.spice", "content_hash": "sha256:b8b230603cfc88b05ebe27eb18e476ddbbfa0cfab64c23677b2862ae8c658e0e" },
      { "path": "design/netlist/dplus_pullup.spice", "content_hash": "sha256:9c645ecf0da6d80fb05d85c33d2941cc48bc328a00cd427f0c11bdf980b2cceb" },
      { "path": "design/netlist/se_receiver_dm.spice", "content_hash": "sha256:ebfc91ac1306060dfa0ffce7d91e048b4fc8b50cfebbd5804c3aaf9b58a20554" },
      { "path": "design/netlist/se_receiver_dp.spice", "content_hash": "sha256:1f854210680331a1df73b96b317479cad4af325192fec68b0b61fb1421fac0b3" }
    ]
  }
}
-->

# Record 20260909-213500-29c5780

- **Record ID**: 20260909-213500-29c5780
- **Claim**: Moving this repo's `klt` pin from
  `07b1f04f29f21b3f8551d3937183b206a57a5e3a` (`klt 0.3.0+g07b1f04f29f2`) to
  `8bd3f0b09a87641b093d655a7282b8f730db19e8` (`klt 0.4.0+g8bd3f0b09a87`) —
  **with all four committed plan documents byte-identical** — changes the
  measured analog-layout result: the three receiver plans each go from
  **0 routed nets to 1** (`VDD`, in all three), `dplus_pullup` stays at
  **6/21**, all four remain DRC-clean with an empty violation *list*, and
  `differential_driver` still fails ingestion with the identical `rm1`
  error. This record exists so the pin bump's effect is measured **before**
  any plan edit can be credited with it; the plan edit itself is the
  successor record, `20260909-215500-29c5780`.
- **Design provenance**: schematic — `design/*.sch` exported to
  `design/netlist/*.spice` by `design/netlist.py`. **No design source and no
  plan document changed relative to the superseded record**: all five block
  netlists, `design/dplus_pullup.sch`, `scripts/gen_analog_layout.py`, and
  all four `layout/analog/plans/*.json` carry content hashes byte-identical
  to `20260906-004835-90e442a`'s (compare the `inputs[]` arrays directly).
  The **only** changed input in this repo is `scripts/setup-env.sh`, and the
  only thing changed inside it is `KLT_REV`. Everything this record reports
  is therefore attributable to the tool, not to the design.
- **Run configuration**: `PDK_ROOT=$HOME/.volare .venv/bin/python
  scripts/gen_analog_layout.py --format json` — the same one command, same
  invocation — from a **freshly created** `./scripts/setup-env.sh` venv
  (`rm -rf .venv` first, so no stale `klt` could survive the bump) at the new
  `KLT_REV`, plus a direct `klt drc --deck gf180mcu --format json` per
  executed block to retain the full violation list (the script's aggregated
  `by_rule` counts do not carry individual violation records). `npm run
  check:ci` was run at the new pin **before** the pin was committed and
  passed: all 10 `klt functional-verification` requests `pass`, 65/65 cocotb
  tests `passed`, 0 failures.
- **Statistical convention**: N/A — deterministic runs against fixed
  netlists and fixed plan documents.
- **Pin ancestry verified, not assumed.** Both upstream fixes this move
  exists to pick up are **strict ancestors** of the new pin, checked with
  `gh api repos/2AMLogic/klayout-tools/compare/<fix>...8bd3f0b09a87641b093d655a7282b8f730db19e8`:
  klayout-tools#1502 / PR #1509 (`815d8abadf4182bd1b2543c1dee2f7d04a5ee904`,
  2026-09-06T01:20:17Z) → `status: "ahead"`, `behind_by: 0`, `ahead_by: 83`;
  klayout-tools#1501 / PR #1507 (`8d094b70628aae2d4cdba7544989c415c1f877a4`,
  2026-09-06T01:05:05Z) → `status: "ahead"`, `behind_by: 0`, `ahead_by: 84`.
  The old pin `07b1f04f` is also `behind_by: 0` / `ahead_by: 189` relative to
  the new one, so this is a strict fast-forward and not a divergent branch.
- **Result**: **The pin bump alone is not a no-op. Three of the four
  executable blocks gained a routed net; `dplus_pullup` did not.**

  | Block | Superseded run (`klt 0.3.0+g07b1f04f29f2`) | This run (`klt 0.4.0+g8bd3f0b09a87`), plans byte-identical |
  |---|---|---|
  | `differential_receiver` | 11 groups, 0/8 nets, 0 violations | 11 groups, **1/8 nets** (`VDD`), 0 violations |
  | `se_receiver_dm` | 13 groups, 0/9 nets, 0 violations | 13 groups, **1/9 nets** (`VDD`), 0 violations |
  | `se_receiver_dp` | 13 groups, 0/9 nets, 0 violations | 13 groups, **1/9 nets** (`VDD`), 0 violations |
  | `dplus_pullup` | 24 groups, 6/21 nets, 0 violations | 24 groups, **6/21 nets (unchanged)**, 0 violations |
  | `differential_driver` | ingestion failed (`rm1` unknown device) | ingestion failed, **identical error text**, verbatim |

  1. **The full violation list, not just the count, is empty for all four
     executed blocks.** Direct `klt drc --deck gf180mcu --format json`
     against each GDS returns `"violations": []` with `"rule_counts": {}` —
     read from the full JSON array, not inferred from
     `violation_count: 0`. Frozen per block as
     `<block>-drc-full.json` under this record's artifacts.
  2. **Geometry moved, and that is the mechanism.** Every block's `bbox_um`
     and `device_groups[]` placement differ from the superseded record's
     (e.g. `differential_receiver` 433.68 × 41.12 µm → 439.08 × 41.82 µm;
     `dplus_pullup` `x1` 201.939 → 218.609 µm, `y1` 101.12 → 101.82 µm, and
     `x0` 0.0 → −0.25 µm). The **deck did not change** — its content hash is
     byte-identical to the superseded record's
     (`sha256:79e71a1e…`) — so this is klayout-tools' own generators
     re-flooring drawn geometry at the deck's DRC minimums
     (klayout-tools#1501 / PR #1507), not a PDK or deck movement.
  3. **The routed net in all three receivers is `VDD`, and the three are
     otherwise unmoved.** Net-by-net status is now
     `{VDD: routed, AMPOUT: partial, IBIASN: partial, rest: unrouted}` in
     each of the three; no other net changed class. Nothing about the
     receiver *plans* was touched — this record does not re-author them, and
     re-authoring them in `dplus_pullup`'s grouped shape remains
     out of scope here.
  4. **`differential_driver`'s ingestion blocker is reconfirmed, verbatim,
     at the new pin**: `subcircuit 'rm1' is not a known device for the
     requested deck (known: cap_mim_2f0_m4m5_noshield, nfet_03v3,
     nfet_06v0, pfet_03v3, pfet_06v0, ppolyf_u, ppolyf_u_1k, ppolyf_u_2k,
     ppolyf_u_3k); if it is a real device, pass reference.device_map to map
     it explicitly`. No metal-resistor device class appeared in the two pin
     moves' worth of upstream work. Filed upstream as a generic tool gap by
     this issue's run (see Links) rather than worked around here — a
     flattened or substituted device would be a `spec/decisions/` matter,
     per #56's precedent.
  5. **The router's rejection reporting got materially better at this pin**,
     which is why the successor record can name a per-net cause at all: each
     `nets[].legs[]` entry now carries a `reason` string that names the
     obstructing block, the crossed distance, the drawn width, and — for a
     same-block self-net short — the remedy by field name (`route to a
     layer_role with a metal2/via stack instead (or configure
     routing.cross_block_layer_role, issue #1168)`). That string is what
     `20260909-215500-29c5780` acts on.

  **What this record does and does not claim.** It claims the pin move is
  measured, isolated to one changed repo input, and produced a small but
  real routing gain in three blocks with no DRC regression anywhere. It does
  **not** claim any block is a delivered layout: the best block still routes
  6 of 21 nets, the receivers 1 of 8/9/9, no analog GDS is committed under
  `layout/`, and a block whose supplies and ladder nodes are unwired is not
  a layout under this repo's own rule (`layout/README.md`).
- **klt provenance**: `klt` 0.4.0+g8bd3f0b09a87 @ klayout-tools
  `8bd3f0b09a87641b093d655a7282b8f730db19e8` (this repo's newly pinned
  `KLT_REV`, provisioned into a from-scratch `.venv` by
  `scripts/setup-env.sh` for this run), `klayout` 0.30.12; PDK `gf180mcuD`,
  open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`; DRC deck `gf180mcu`,
  content hash
  `sha256:79e71a1e7d84be3cfc82e4c70afbdf7b743ac1f361fb8e981f57831014d2e8b0`
  (byte-identical to the superseded record's deck build — the deck did not
  move with the pin). klt drove both legs of this run — plan execution and
  DRC — for all five blocks; nothing here is hand-derived.
- **Links**:
  - Driver: `scripts/gen_analog_layout.py` (unchanged)
  - Changed input: `scripts/setup-env.sh` (`KLT_REV` only)
  - Pin history entry: `docs/environment-setup.md` § "Pinned versions", move 3
  - Plans under test (all four unchanged, byte-identical to the superseded
    record): `layout/analog/plans/*.json`
  - Designs under test: `design/netlist/*.spice` — none changed
  - Raw artifacts:
    `verification/records/analog-layout/artifacts/20260909-213500-29c5780/`
    — `analog-layout-report.json` (the combined report); one
    `<block>-layout-plan.json`, `<block>.gds` and `<block>-drc-full.json`
    per executed block (`differential_receiver`, `se_receiver_dm`,
    `se_receiver_dp`, `dplus_pullup`).
  - Upstream fixes this pin move picks up:
    [klayout-tools#1502](https://github.com/2AMLogic/klayout-tools/issues/1502)
    (PR #1509, `815d8ab`) and
    [klayout-tools#1501](https://github.com/2AMLogic/klayout-tools/issues/1501)
    (PR #1507, `8d094b7`).
  - Upstream gap reconfirmed by this run and filed generically:
    [klayout-tools#1621](https://github.com/2AMLogic/klayout-tools/issues/1621)
    — no metal-resistor device class, so a subckt-call netlist referencing
    one cannot be ingested at all, and no `klt gen` generator draws one.
  - Succeeded by: `verification/records/analog-layout/records/20260909-215500-29c5780.md`
    — same driver, same pin, same design sources; that record changes
    `layout/analog/plans/dplus_pullup.json` and is where
    `routing.cross_block_layer_role` is spent.
  - Superseded: `verification/records/analog-layout/records/20260906-004835-90e442a.md`
    — same five-block claim, same driver, same design sources; superseded
    because the `klt` pin (and therefore the measured result) changed, not
    because anything it reported was wrong.
- **Timestamp / author**: 2026-09-09T21:35:00Z, Loom Builder (AI agent),
  issue #68.
- **Supersedes**: 20260906-004835-90e442a — same five-block claim, same
  driver, same plan documents and design sources. The changed input is
  `scripts/setup-env.sh`'s `KLT_REV`, which is exactly why that record's
  `klt_version` provenance is now stale and this record exists.
