<!-- record-meta
{
  "record_id": "20260914-023747-74372a3",
  "experiment": "analog-layout",
  "supersedes": "20260913-192744-b653c71",
  "git_revision": "74372a3ff6858947ea566f758949c152894e3ffa",
  "provenance": {
    "klt_version": "0.4.0+g61e743088f47",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:3c2f4e4be2524a0fe964cd1b2c4c171c0f904586c10eaa8c4ffe3511909e4706"
    },
    "inputs": [
      { "path": "scripts/setup-env.sh", "content_hash": "sha256:f0f49c5052c204afc9542fce6f8a154df8fca17eaa5acc4b2bf9ce5c635d4422" },
      { "path": "scripts/gen_analog_layout.py", "content_hash": "sha256:ff45c4e289284df5c9427f4884fc2b446acd57440a685ac8ae24ec7b75c3865d" },
      { "path": "layout/analog/plans/differential_receiver.json", "content_hash": "sha256:77c62e64b32eeb81732dedd2ec841a6e584aa64bcebbd598cd0586081694abdf" },
      { "path": "layout/analog/plans/dplus_pullup.json", "content_hash": "sha256:0f7dac6b6c74df0a171b57d8c7df20faf943eb9a8be12d9a2fc6d88e8c68fa31" },
      { "path": "layout/analog/plans/se_receiver_dm.json", "content_hash": "sha256:90b3ec9e6b22ce691540746afc85731e3d9692c94a63028167049b9afbf5a687" },
      { "path": "layout/analog/plans/se_receiver_dp.json", "content_hash": "sha256:4806a6fa835c72f6205d1aef2a77732703f109205bac472a76bfb6b1b961a81a" },
      { "path": "layout/analog/plans/differential_driver.json", "content_hash": "sha256:8d134f5e80e13342878b9e47c3f9670595c3e5f7b7cc842eee114484bfd896c6" },
      { "path": "design/dplus_pullup.sch", "content_hash": "sha256:81eaf5101b343bc5773f7d2800ea3dec40f8d7a5388240b8d22fd13a0b7684e6" },
      { "path": "design/netlist/differential_driver.spice", "content_hash": "sha256:bd4b2608c986fd1e305be7b1522e94f578cf36206fe30c13325f48a55f46afa5" },
      { "path": "design/netlist/differential_receiver.spice", "content_hash": "sha256:b8b230603cfc88b05ebe27eb18e476ddbbfa0cfab64c23677b2862ae8c658e0e" },
      { "path": "design/netlist/dplus_pullup.spice", "content_hash": "sha256:9c645ecf0da6d80fb05d85c33d2941cc48bc328a00cd427f0c11bdf980b2cceb" },
      { "path": "design/netlist/se_receiver_dm.spice", "content_hash": "sha256:ebfc91ac1306060dfa0ffce7d91e048b4fc8b50cfebbd5804c3aaf9b58a20554" },
      { "path": "design/netlist/se_receiver_dp.spice", "content_hash": "sha256:1f854210680331a1df73b96b317479cad4af325192fec68b0b61fb1421fac0b3" }
    ]
  }
}
-->

# Record 20260914-023747-74372a3

- **Record ID**: 20260914-023747-74372a3
- **Claim**: The seven `res_array` groups record `20260913-192744-b653c71`
  flagged (`differential_receiver.json`'s `r_bias`; `se_receiver_dm.json`/
  `se_receiver_dp.json`'s `r_bias`/`r_1`/`r_2`) now carry `params.flavor:
  "1k"`, and re-executing all three plans confirms this fixes exactly the
  bug filed: each affected group's `klt gen`-drawn body swaps from the
  default `"generic"` flavor (`ppolyf_u`, GDS layer `31/0`) to the netlist's
  actually-declared `PPOLYF_U_1K` flavor (`ppolyf_u_1k`, layer `49/0` body +
  `62/0` sheet-resistance marker) — confirmed by direct GDS layer inspection
  of each affected group's own bounding box, not inferred from the absence
  of an error. The `_device_class_mismatch_warnings` check that surfaced the
  bug now reports zero `warnings[]` entries for all three plans (down from
  1/3/3). **Nothing else moved**: routed-net counts, DRC status, device
  counts, and overall block bboxes are byte-for-byte unchanged from the
  superseded record, and a per-layer Boolean XOR against that record's
  frozen GDS artifacts confirms the *only* layers with any area difference
  in any of the three blocks are `31/0` (removed) and `62/0` (added), with
  equal area on each per block — i.e. the fix is exactly and only the
  resistor-body flavor swap, with everything else (placement, MOS devices,
  routing, DRC-relevant geometry) provably untouched.
- **Design provenance**: schematic — `design/*.sch` exported to
  `design/netlist/*.spice` by `design/netlist.py`. **No design source
  changed relative to the superseded record**: all five block netlists and
  `design/dplus_pullup.sch` carry content hashes byte-identical to
  `20260913-192744-b653c71`'s (compare the `inputs[]` arrays directly). The
  `klt` pin (`scripts/setup-env.sh`) and the driver script
  (`scripts/gen_analog_layout.py`) are also byte-identical to that record —
  the only changed inputs are the three plan documents this issue's scope
  names: `layout/analog/plans/differential_receiver.json`,
  `layout/analog/plans/se_receiver_dm.json`, and
  `layout/analog/plans/se_receiver_dp.json` (each gains `params.flavor:
  "1k"` on its `PPOLYF_U_1K`-declared `res_array` group(s), and nothing
  else — no field added, removed, or reordered elsewhere in any of the
  three files).
- **Run configuration**: `PDK_ROOT=$HOME/.volare .venv/bin/python
  scripts/gen_analog_layout.py --format json`, from a `./scripts/setup-env.sh`
  venv provisioned fresh in this worktree at the unchanged `KLT_REV`
  (`61e743088f4722854d18b0cc48bd98c3dee58cf6`), plus a direct
  `klt drc --deck gf180mcu --format json` per executed block (all five, this
  run, matching the superseded record's convention) and three additional
  direct checks beyond the harness's own summary, to establish the
  flavor-fix claim at the same rigor `differential_driver`'s `metal_level`
  fix was held to in the superseded record:

  1. Each affected plan's `resolved_params` for its `PPOLYF_U_1K`-declared
     group(s) now includes `"flavor": "1k"` (e.g.
     `differential_receiver.json`'s `r_bias`:
     `{"length_um": 400.0, "width_um": 2.0, "num": 1, "dummy": 0, "flavor":
     "1k"}`) — the plan's own explicit override, not klt's default.
  2. `response["warnings"]` is `[]` for all three affected plans (down from
     1, 3, 3 respectively in the superseded record) — the same
     `_device_class_mismatch_warnings` check confirms no remaining
     disagreement for any of the seven groups.
  3. Reading each affected block's generated GDS directly with
     `klayout.db.Layout.read()` and intersecting every layer against each
     fixed group's own `bbox_um` (from its `resolved_params` response):
     layer `31/0` (the `ppolyf_u`/"generic"-flavor body,
     `gen._PDK_RES_FLAVOR_LAYERS["gf180mcu"]["generic"]`) is present in the
     superseded record's frozen artifact for every one of the seven groups
     and absent from this run's GDS for all seven; layer `62/0` (the
     `"1k"`-flavor sheet-resistance marker,
     `gen._PDK_RES_FLAVOR_LAYERS["gf180mcu"]["1k"]`) is the reverse — absent
     in the superseded artifact, present in this run — confirmed by
     reading `klayout_tools.gen._PDK_RES_FLAVOR_LAYERS["gf180mcu"]` directly
     from the installed package, not assumed. Layer `49/0` (shared between
     both flavors) is present in both, as expected. No other layer differs
     inside any of the seven groups' bounding boxes.
- **Statistical convention**: N/A — deterministic runs against fixed
  netlists and fixed plan documents.
- **Result**: **The flavor fix lands exactly as scoped: all seven affected
  `res_array` groups now draw the netlist-declared `ppolyf_u_1k` body, the
  mismatch-warning check confirms it (0 `warnings[]`, all three plans), and
  DRC stays clean with routed-net counts and bboxes unchanged on all three
  blocks.**

  | Block | Superseded run (`20260913-192744-b653c71`) | This run |
  |---|---|---|
  | `differential_receiver` | 11 groups, 1/8 nets (`VDD`), 0 violations, **1 `warnings[]` entry** (`r_bias`) | unchanged group/net/DRC counts and bbox (439.08 × 41.82 µm); **0 `warnings[]`**; `r_bias` now draws `ppolyf_u_1k` (layer swap `31/0`→`62/0` confirmed by direct GDS read, zero XOR area outside that swap) |
  | `se_receiver_dm` | 13 groups, 1/9 nets (`VDD`), 0 violations, **3 `warnings[]` entries** (`r_1`, `r_2`, `r_bias`) | unchanged group/net/DRC counts and bbox (451.36 × 41.82 µm); **0 `warnings[]`**; all three groups now draw `ppolyf_u_1k` (same layer-swap confirmation) |
  | `se_receiver_dp` | 13 groups, 1/9 nets (`VDD`), 0 violations, **3 `warnings[]` entries** (`r_1`, `r_2`, `r_bias`) | unchanged group/net/DRC counts and bbox (451.36 × 41.82 µm); **0 `warnings[]`**; all three groups now draw `ppolyf_u_1k` (same layer-swap confirmation) |
  | `differential_driver` | 12 groups, 0/10 nets, 0 violations, 0 `warnings[]` (unaffected by this issue) | `cmp`-identical GDS to the superseded artifact — plan untouched, confirmed byte-for-byte, not just "unchanged by report summary" |
  | `dplus_pullup` | 24 groups, 6/21 nets, 0 violations, 0 `warnings[]` (unaffected — its `res_array` groups already declared plain `PPOLYF_U`, matching the default flavor) | `cmp`-identical GDS to the superseded artifact — plan untouched, confirmed byte-for-byte |

  1. **Per-layer Boolean XOR against the superseded record's frozen
     artifacts is exactly zero on every layer except `31/0` and `62/0`, for
     all three affected blocks** — the stricter check, not a byte
     comparison (these two GDS files are *not* byte-identical to each
     other's superseded versions, since the resistor body genuinely
     changed; `cmp` correctly reports a difference for all three, unlike
     `differential_driver`/`dplus_pullup` above, whose plans were untouched
     and whose GDS *is* byte-identical). The two nonzero layers have equal
     area to each other within each block (`differential_receiver`:
     3,200,000,000 dbu² on each of `31/0` and `62/0`; `se_receiver_dm`/
     `se_receiver_dp`: 3,252,800,000 dbu² on each) — a pure body-layer swap
     with no net area gained or lost, consistent with the same resistor
     footprint being redrawn in a different flavor's layer set rather than
     resized.
  2. **DRC stays clean.** `klt drc --deck gf180mcu --format json` against
     each of this run's three regenerated GDS files reports `status:
     "clean"`, `violation_count: 0` — identical to the superseded record.
     The DRC deck's own `content_hash` (`sha256:3c2f4e4b…`) is unchanged
     (same `klt` pin, same curated deck).
  3. **Routed-net counts and device-group counts are unchanged**: 1/8,
     1/9, 1/9 (all `VDD`) and 11/13/13 groups respectively, matching the
     superseded record exactly. This fix only changes which body a `res_array`
     group draws for a resistor that was already placed and already
     unrouted (or routed) the same way — it does not touch placement,
     routing, or any other device.
  4. **Not attempted, deliberately out of scope**: routing any of the
     newly-visible-as-still-unrouted nets on any of the three blocks (the
     issue's own "Out of Scope" section, and this repo's `CLAUDE.md`
     scope-discipline convention against letting a plan-data fix grow into
     unrelated layout design work). No newly-unrouted net was introduced by
     this fix (the routed-net counts are unchanged, item 3), so there is
     nothing new to route as a consequence of this change specifically —
     the pre-existing 7/8, 8/9, 8/9 unrouted nets on these three blocks
     remain exactly what they were, tracked by the analog-layout narrative
     in `layout/README.md`, not by this record.
- **klt provenance**: `klt` 0.4.0+g61e743088f47 @ klayout-tools
  `61e743088f4722854d18b0cc48bd98c3dee58cf6` (unchanged from the superseded
  record — this issue does not move the pin), `klayout` 0.30.12; PDK
  `gf180mcuD`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`
  (unchanged); DRC deck `gf180mcu`, content hash
  `sha256:3c2f4e4be2524a0fe964cd1b2c4c171c0f904586c10eaa8c4ffe3511909e4706`
  (unchanged), `released: false`. klt drove every leg of this run (plan
  execution for all five blocks, DRC, and the direct GDS-layer/
  `resolved_params`/`warnings[]` checks on the three affected blocks);
  nothing here is hand-derived.
- **Links**:
  - Fixed inputs: `layout/analog/plans/differential_receiver.json`,
    `layout/analog/plans/se_receiver_dm.json`,
    `layout/analog/plans/se_receiver_dp.json` (each gains `params.flavor:
    "1k"` on its `PPOLYF_U_1K`-declared `res_array` group(s))
  - Unchanged: `scripts/setup-env.sh`, `scripts/gen_analog_layout.py`,
    `layout/analog/plans/differential_driver.json`,
    `layout/analog/plans/dplus_pullup.json`, all five `design/netlist/*.spice`
  - Designs under test: `design/netlist/*.spice` — none changed
  - Raw artifacts:
    `verification/records/analog-layout/artifacts/20260914-023747-74372a3/`
    — `analog-layout-report.json` (the combined report); one
    `<block>-layout-plan.json`, `<block>.gds` and `<block>-drc-full.json`
    per executed block (all five)
  - Documentation updated: `layout/README.md` § "Analog" per-block table
    gains a new run column reflecting this fix; the "New finding, ninth
    run" / item (f) prose is updated to point at this record as the fix
    rather than leaving the bug open-ended
  - Tracking issue: [gf180-usb2-phy#73](https://github.com/2AMLogic/gf180-usb2-phy/issues/73)
  - Superseded: `verification/records/analog-layout/records/20260913-192744-b653c71.md`
    — same five-block claim, minus this record's flavor fix; superseded
    because three of its five plan documents changed (the fix this record
    exists to measure). Not edited or deleted — its "New finding, ninth
    run" is the correct account of how the bug was first found, and this
    record picks up from exactly that finding.
- **Timestamp / author**: 2026-09-14T02:37:47Z, Loom Builder (AI agent),
  issue #73.
- **Supersedes**: 20260913-192744-b653c71 — mechanically superseded in
  full (three of its five tracked plan-document inputs changed, so its own
  provenance hashes for those inputs are now stale against the working
  tree per this repo's evidence-freshness check), but its substantive
  claims about `differential_driver`'s `metal_level` fix, the byte-order
  (not geometric) nature of the pin-move GDS diff, and the discovery
  narrative for this very bug remain the correct, standing account of
  those events — nothing in this record contradicts them, this record only
  supplies the fix and re-measurement `20260913-192744-b653c71` explicitly
  deferred (its own "Result" item 3) to a separate, tracked issue.
