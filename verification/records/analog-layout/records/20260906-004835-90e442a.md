<!-- record-meta
{
  "record_id": "20260906-004835-90e442a",
  "experiment": "analog-layout",
  "supersedes": "20260905-233520-2eca93d",
  "git_revision": "90e442aead4f2dbfdb78b1330abc635eb5e160f3",
  "provenance": {
    "klt_version": "0.3.0+g07b1f04f29f2",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:79e71a1e7d84be3cfc82e4c70afbdf7b743ac1f361fb8e981f57831014d2e8b0"
    },
    "inputs": [
      { "path": "scripts/gen_analog_layout.py", "content_hash": "sha256:65b7c20e3f852fabadfd4e69a40d9f9794dc18a32c488b42540af5e973abd5d9" },
      { "path": "layout/analog/plans/differential_receiver.json", "content_hash": "sha256:f04c6d416066179975458c7c253db9f6143b3f0e1a98cb4519692a9e5871de1c" },
      { "path": "layout/analog/plans/dplus_pullup.json", "content_hash": "sha256:8d66acb201f3c446e5f27fda9fb096b4da760b8a5aef30d54a6c0714f8168782" },
      { "path": "layout/analog/plans/se_receiver_dm.json", "content_hash": "sha256:4f8d5b08e4007e851fa1095528b40bb926c4fe44864af8cb886472335b810b36" },
      { "path": "layout/analog/plans/se_receiver_dp.json", "content_hash": "sha256:4d16672ea830a0d695ee62edab0b713046854aedc873f9ba67c57152869cec8b" },
      { "path": "design/dplus_pullup.sch", "content_hash": "sha256:81eaf5101b343bc5773f7d2800ea3dec40f8d7a5388240b8d22fd13a0b7684e6" },
      { "path": "design/netlist/differential_driver.spice", "content_hash": "sha256:bd4b2608c986fd1e305be7b1522e94f578cf36206fe30c13325f48a55f46afa5" },
      { "path": "design/netlist/differential_receiver.spice", "content_hash": "sha256:b8b230603cfc88b05ebe27eb18e476ddbbfa0cfab64c23677b2862ae8c658e0e" },
      { "path": "design/netlist/dplus_pullup.spice", "content_hash": "sha256:9c645ecf0da6d80fb05d85c33d2941cc48bc328a00cd427f0c11bdf980b2cceb" },
      { "path": "design/netlist/se_receiver_dm.spice", "content_hash": "sha256:ebfc91ac1306060dfa0ffce7d91e048b4fc8b50cfebbd5804c3aaf9b58a20554" },
      { "path": "design/netlist/se_receiver_dp.spice", "content_hash": "sha256:1f854210680331a1df73b96b317479cad4af325192fec68b0b61fb1421fac0b3" }
    ]
  }
}
-->

# Record 20260906-004835-90e442a

- **Record ID**: 20260906-004835-90e442a
- **Claim**: `layout/analog/plans/{differential_receiver,se_receiver_dm,se_receiver_dp}.json`
  now each carry an explicit `routing` block (`layer_role: "metal"`,
  `width_um: 0.23` — the `gf180mcu` deck's own `metal1.width.1` minimum,
  re-confirmed directly against the installed deck at build time: see
  "Result" below) instead of taking `klt`'s illegal 0.17 µm default.
  Re-executing all three plans against the current `klt` pin clears every
  `metal1.width.1` violation (19 → 0, 22 → 0, 22 → 0) with **routed-net
  counts unchanged** at 0/8, 0/9, 0/9, and the full DRC violation list (not
  just the `metal1.width.1` count) is empty for all three — no new
  violation class (e.g. `metal1.space.1`) appears at the legal width. This
  is issue gf180-usb2-phy#65, the follow-up named but deliberately not
  folded into #62/`dplus_pullup`'s plan (record `20260905-233520-2eca93d`)
  or into this repo's earlier `metal1.width.1`/klayout-tools#1424
  misattribution.
- **Design provenance**: schematic — `design/*.sch` exported to
  `design/netlist/*.spice` by `design/netlist.py`. **No design source
  changed relative to the superseded record**: all five block netlists and
  `design/dplus_pullup.sch` carry byte-identical content hashes to
  `20260905-233520-2eca93d`'s. `scripts/gen_analog_layout.py` and
  `layout/analog/plans/dplus_pullup.json` are also byte-identical (same
  hashes). The only changed inputs are the three plan documents this record
  targets — each gains a `routing` block and nothing else (device groups,
  `rows[]` order/spacing, and `options` are untouched; diffed directly
  against the superseded record's committed plan content).
- **Run configuration**: `PDK_ROOT=$HOME/.volare .venv/bin/python
  scripts/gen_analog_layout.py --format json` from a fresh
  `./scripts/setup-env.sh` venv at the repo's pinned `KLT_REV`
  (`07b1f04f29f21b3f8551d3937183b206a57a5e3a`, identical to the superseded
  record), plus a direct `klt drc --deck gf180mcu --format json` per
  executed block to retain the full violation list (the script's aggregated
  `by_rule` counts do not carry individual violation records).
- **Statistical convention**: N/A — deterministic runs against fixed
  netlists and fixed plan documents.
- **Deck minimum re-confirmed at build time, not trusted from the issue
  body.** `klayout_tools.decks.gf180mcu` declares `metal1.width.1` at
  `threshold_dbu: 230` (0.23 µm, DRM 7.13 "Mn.1") in the exact `klt` build
  this record's run used — read directly from the installed package, not
  assumed from the issue text. `width_um: 0.23` sits exactly at that
  minimum (not below it), and `klt`'s own hard-coded hard default
  (`layout_plan_execute._DEFAULT_ROUTING`, `gen_compose.py`) is confirmed
  `{"layer_role": "metal", "width_um": 0.17}` — the value the three plans
  took by omission before this record.
- **Result**: **All three plans are now DRC-clean, with routed-net counts
  provably unchanged.**

  | Block | Superseded run (no `routing` block, default 0.17 µm) | This run (`routing.width_um: 0.23`) |
  |---|---|---|
  | `differential_receiver` | 11 groups, 0/8 nets, 19 × `metal1.width.1` | 11 groups, **0/8 nets (unchanged)**, **0 violations** |
  | `se_receiver_dm` | 13 groups, 0/9 nets, 22 × `metal1.width.1` | 13 groups, **0/9 nets (unchanged)**, **0 violations** |
  | `se_receiver_dp` | 13 groups, 0/9 nets, 22 × `metal1.width.1` | 13 groups, **0/9 nets (unchanged)**, **0 violations** |
  | `dplus_pullup` | 24 groups, 6/21 nets, 0 violations | unchanged — byte-identical GDS and byte-identical full DRC violation list (both empty) |
  | `differential_driver` | ingestion failed (`rm1` unknown device) | ingestion failed, identical error text, verbatim |

  1. **Routed-net counts and placement are unchanged — measured, not just
     asserted.** For each of the three target blocks, this run's
     `<block>-layout-plan.json` `nets[]` (`routed`/`name`), `unrouted_nets`,
     `device_groups[]` (placement), and `bbox_um` are byte-for-byte equal to
     the superseded record's own `<block>-layout-plan.json`. Only the routed
     geometry's drawn width changed; nothing about *what* is routed moved.
  2. **The full violation list, not just the count, is empty.** Direct
     `klt drc --deck gf180mcu --format json` against each of the three
     blocks' GDS returns `"violations": []` — confirmed by inspecting the
     full JSON array, not by trusting the aggregated `violation_count: 0`
     alone, so no new violation class (`metal1.space.1` or otherwise)
     appeared at the tighter/legal width. The prior violations on all three
     blocks were exclusively `metal1.width.1` (19/22/22, re-verified against
     the superseded record's own frozen `<block>-drc-full.json`), which is
     exactly the class this fix targets.
  3. **`dplus_pullup` and `differential_driver` are demonstrably
     unaffected.** `dplus_pullup.gds` is `cmp`-byte-identical to the
     superseded record's committed artifact, and its full DRC violation list
     (already empty in both) is unchanged; `differential_driver`'s ingestion
     failure reproduces with the identical error text. Neither block's plan
     was touched by this change.
  4. **This closes the last open item from `20260905-233520-2eca93d`'s "What
     this record does and does not claim":** that record explicitly stated
     it did "not claim the two older receiver plans are fixed" and pointed
     at this issue. All three (not just the two `se_receiver_*` blocks —
     `differential_receiver` as well) are now fixed by the same mechanism
     and the same value that record measured for `dplus_pullup`.

  **What this record does and does not claim.** It claims the three older
  analog-layout plans are DRC-clean with their routed-net counts unchanged
  — a routing-*attribution* correction, not a routing improvement. It does
  **not** claim any of the three is a delivered layout: `differential_receiver`
  still routes 0 of 8 nets, and both `se_receiver_dm`/`se_receiver_dp` still
  route 0 of 9 — DRC-clean placement with nothing wired is not a layout
  under this repo's own rule (`layout/README.md`), and no analog GDS is
  committed under `layout/`.
- **klt provenance**: `klt` 0.3.0+g07b1f04f29f2 @ klayout-tools
  `07b1f04f29f21b3f8551d3937183b206a57a5e3a` (this repo's pinned `KLT_REV`,
  identical to the superseded record), `klayout` 0.30.12; PDK `gf180mcuD`,
  open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`; DRC deck `gf180mcu`,
  content hash
  `sha256:79e71a1e7d84be3cfc82e4c70afbdf7b743ac1f361fb8e981f57831014d2e8b0`
  (byte-identical to the superseded record's deck build). klt drove both
  legs of this run — plan execution and DRC — for all five blocks; nothing
  here is hand-derived.
- **Links**:
  - Driver: `scripts/gen_analog_layout.py` (unchanged)
  - Plans under test (changed — `routing` block added):
    `layout/analog/plans/{differential_receiver,se_receiver_dm,se_receiver_dp}.json`
  - Plan under test (unchanged): `layout/analog/plans/dplus_pullup.json`
  - Designs under test: `design/netlist/*.spice` — none changed
  - Diagnosis + friction: `layout/README.md` § "Analog"
  - Raw artifacts:
    `verification/records/analog-layout/artifacts/20260906-004835-90e442a/`
    — `analog-layout-report.json` (the combined report); one
    `<block>-layout-plan.json`, `<block>.gds` and `<block>-drc-full.json`
    per executed block (`differential_receiver`, `se_receiver_dm`,
    `se_receiver_dp`, `dplus_pullup`).
  - Upstream tool gap this record's fix works around (not filed fresh here —
    already open from #62's run):
    [klayout-tools#1501](https://github.com/2AMLogic/klayout-tools/issues/1501)
    — neither `routing.width_um` nor the via-drop square is checked against
    the resolved PDK deck's own minimum-width rules.
  - Prior issue this record closes: gf180-usb2-phy#65 (re-author the three
    older plans' routing width; filed and scoped by #62's
    `20260905-233520-2eca93d`).
  - Superseded: `verification/records/analog-layout/records/20260905-233520-2eca93d.md`
    — same five-block claim, same driver, same `klt` pin, same design
    sources. That record remains the correct, unedited account of what the
    repo measured when only `dplus_pullup` carried an explicit `routing`
    block; it is superseded because the three older plans' content (and
    therefore their DRC result) has changed, not because anything it
    reported was wrong.
- **Timestamp / author**: 2026-09-06T00:48:35Z, Loom Builder (AI agent),
  issue #65.
- **Supersedes**: 20260905-233520-2eca93d — same five-block claim and same
  driver/`klt` pin/design sources; the three older plan documents are the
  changed inputs, which is exactly why that record's provenance hashes for
  them are now stale and this record exists.
