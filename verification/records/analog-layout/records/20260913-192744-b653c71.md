<!-- record-meta
{
  "record_id": "20260913-192744-b653c71",
  "experiment": "analog-layout",
  "supersedes": "20260912-191922-5953e89",
  "git_revision": "b653c714fc1caeb2d7abcfcacea23b60590ddeeb",
  "provenance": {
    "klt_version": "0.4.0+g61e743088f47",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:3c2f4e4be2524a0fe964cd1b2c4c171c0f904586c10eaa8c4ffe3511909e4706"
    },
    "inputs": [
      { "path": "scripts/setup-env.sh", "content_hash": "sha256:f0f49c5052c204afc9542fce6f8a154df8fca17eaa5acc4b2bf9ce5c635d4422" },
      { "path": "scripts/gen_analog_layout.py", "content_hash": "sha256:ff45c4e289284df5c9427f4884fc2b446acd57440a685ac8ae24ec7b75c3865d" },
      { "path": "layout/analog/plans/differential_receiver.json", "content_hash": "sha256:f04c6d416066179975458c7c253db9f6143b3f0e1a98cb4519692a9e5871de1c" },
      { "path": "layout/analog/plans/dplus_pullup.json", "content_hash": "sha256:0f7dac6b6c74df0a171b57d8c7df20faf943eb9a8be12d9a2fc6d88e8c68fa31" },
      { "path": "layout/analog/plans/se_receiver_dm.json", "content_hash": "sha256:4f8d5b08e4007e851fa1095528b40bb926c4fe44864af8cb886472335b810b36" },
      { "path": "layout/analog/plans/se_receiver_dp.json", "content_hash": "sha256:4d16672ea830a0d695ee62edab0b713046854aedc873f9ba67c57152869cec8b" },
      { "path": "layout/analog/plans/differential_driver.json", "content_hash": "sha256:8d134f5e80e13342878b9e47c3f9670595c3e5f7b7cc842eee114484bfd896c6" },
      { "path": "design/dplus_pullup.sch", "content_hash": "sha256:81eaf5101b343bc5773f7d2800ea3dec40f8d7a5388240b8d22fd13a0b7684e6" },
      { "path": "design/netlist/differential_driver.spice", "content_hash": "sha256:bd4b2608c986fd1e305be7b1522e94f578cf36206fe30c13325f48a55f46afa5" },
      { "path": "design/netlist/differential_receiver.spice", "content_hash": "sha256:b8b230603cfc88b05ebe27eb18e476ddbbfa0cfab64c23677b2862ae8c658e0e" },
      { "path": "design/netlist/dplus_pullup.spice", "content_hash": "sha256:9c645ecf0da6d80fb05d85c33d2941cc48bc328a00cd427f0c11bdf980b2cceb" },
      { "path": "design/netlist/se_receiver_dm.spice", "content_hash": "sha256:ebfc91ac1306060dfa0ffce7d91e048b4fc8b50cfebbd5804c3aaf9b58a20554" },
      { "path": "design/netlist/se_receiver_dp.spice", "content_hash": "sha256:1f854210680331a1df73b96b317479cad4af325192fec68b0b61fb1421fac0b3" }
    ]
  }
}
-->

# Record 20260913-192744-b653c71

- **Record ID**: 20260913-192744-b653c71
- **Claim**: Moving this repo's `klt` pin from
  `bf90e1d5e3a624ea2e098efdfe76596081508751` (`klt 0.4.0+gbf90e1d5e3a6`) to
  `61e743088f4722854d18b0cc48bd98c3dee58cf6` (`klt 0.4.0+g61e743088f47`,
  klayout-tools' `main` tip as of this move) — chosen specifically because it
  has klayout-tools PR #1774's merge
  (`1641fa5f9617e57931debbfc2bc58fab62799348`, "populate `res_array`
  `metal_level` for gf180mcu `rm1`/`rm2`/`rm3`, warn on `device_class`
  mismatch") as a strict ancestor — has three effects. **For the four
  already-committed plans, the drawn geometry is a no-op**: identical
  `bbox_um`, identical routed-net counts (1/8, 1/9, 1/9, 6/21), identical
  DRC-clean status, and `cmp`-identical GDS files (verified directly, not
  assumed — see "Result" item 1). **`differential_driver` can now be
  authored, committed, and generated correctly**: with an explicit
  `params.metal_level: 1` on its two `RM1`-classed `res_array` groups, `klt
  gen` now draws real `Metal1`/`Via1`/`Metal2` geometry (layers `34/0`,
  `35/0`, `36/0` — an exact match for `gen.py`'s own
  `_PDK_METAL_RES_LEVELS["gf180mcu"][1]` table) instead of the poly-body
  substitution the superseded record caught, confirmed by direct GDS layer
  inspection, not inferred from the absence of an error (see "Result" item
  2). **A third, unanticipated effect surfaced**: PR #1774's own
  `_device_class_mismatch_warnings` (issue #1731's second half, "warn on
  `device_class` mismatch") is not scoped to `differential_driver` — it
  re-evaluates every `res_array` group in every plan this run executes, and
  found that three of the four already-committed plans
  (`differential_receiver`, `se_receiver_dm`, `se_receiver_dp`) have carried
  the *same class* of declared/drawn resistor mismatch as
  `differential_driver` did, silently, since before this repo's `klt` pin
  first supported detecting it (see "Result" item 3). This is a real,
  previously-undetected finding, not a regression from this pin move — the
  GDS these three plans produce is unchanged (`cmp`-identical, item 1), so
  whatever they have been drawing, they have been drawing since before this
  record; the pin move only made `klt` able to say so.
- **Design provenance**: schematic — `design/*.sch` exported to
  `design/netlist/*.spice` by `design/netlist.py`. **No design source
  changed relative to the superseded record**: all five block netlists and
  `design/dplus_pullup.sch` carry content hashes byte-identical to
  `20260912-191922-5953e89`'s (compare the `inputs[]` arrays directly). The
  changed/added inputs are `scripts/setup-env.sh` (`KLT_REV` only),
  `scripts/gen_analog_layout.py` (`BLOCKED_BLOCKS` emptied + its docstring
  updated for `differential_driver`'s newly-committed plan — no behavior
  change to plan execution or DRC for the four already-executed blocks), and
  the new `layout/analog/plans/differential_driver.json` (committed by this
  record, see "Links").
- **Run configuration**: `PDK_ROOT=$HOME/.volare .venv/bin/python
  scripts/gen_analog_layout.py --format json` from a **freshly created**
  `./scripts/setup-env.sh` venv (`rm -rf .venv` first) at the new `KLT_REV`,
  plus a direct `klt drc --deck gf180mcu --format json` per executed block
  (all five, this run). `npm run check:ci` was run at the new pin **before**
  the pin was committed and passed (see "Links"). For the `differential_driver`
  plan itself, three additional direct checks established the device-class
  claim above beyond what the harness's own summary reports:

  1. `execute_layout_plan_document`'s response for
     `layout/analog/plans/differential_driver.json` carries `resolved_params`
     for both `r_serdm`/`r_serdp` groups of
     `{"length_um": 800.0, "width_um": 2.0, "num": 1, "dummy": 0,
     "metal_level": 1}` — `length_um`/`width_um` auto-populated from the
     netlist digest's `R_length`/`R_width` (800 µm / 2 µm, matching
     `design/netlist/differential_driver.spice`'s `XRSERDP`/`XRSERDM` calls
     exactly), and `metal_level: 1` is the plan's own explicit override, not
     a default.
  2. `response["warnings"]` for this plan is `[]` — the new mismatch check
     (`_device_class_mismatch_warnings`) ran (confirmed by its effect on the
     three other plans below) and found no disagreement for either `RM1`
     group, i.e. `res_array` with `metal_level: 1` on `gf180mcu` is
     confirmed, not assumed, to draw the `rm1` device class the netlist
     declares.
  3. Reading the generated GDS directly with `klayout.db.Layout.read()`:
     layers present include `34/0`, `35/0`, `36/0` (Metal1, Via1, Metal2)
     and `110/11` (the `metal1_res` marker) — `gen.py`'s
     `_PDK_METAL_RES_LEVELS["gf180mcu"][1]` table's exact `body`/`via`/
     `landing`/`marker` layer/datatype quadruple, confirmed by reading that
     table's source directly, not inferred. No poly-resistor layer (the
     `ppolyf_u` PCell's body layer) appears anywhere the two `RM1` groups
     placed. `klt drc --deck gf180mcu` on this GDS reports `status: "clean"`,
     `violation_count: 0`.
- **Statistical convention**: N/A — deterministic runs against fixed
  netlists and fixed plan documents.
- **Pin ancestry verified, not assumed.** `gh api
  repos/2AMLogic/klayout-tools/compare/1641fa5f9617e57931debbfc2bc58fab62799348...61e743088f4722854d18b0cc48bd98c3dee58cf6`
  reports `status: "ahead"`, `ahead_by: 1`, `behind_by: 0` — PR #1774's
  merge commit is a strict ancestor of the new pin. The old pin
  (`bf90e1d5e3a6`) is 32 commits **behind** the fix (`gh api
  repos/2AMLogic/klayout-tools/compare/1641fa5f...bf90e1d5e3a6` →
  `status: "behind"`, `behind_by: 32`), confirming it did not contain the
  fix. **The new pin is klayout-tools' bare `main` tip**
  (`61e743088f4722854d18b0cc48bd98c3dee58cf6`, 2026-09-13T18:59:37Z) — a
  deliberate deviation from this repo's usual "confirm CI, don't just take
  `main`" bar, made because checking both candidates' CI directly showed
  `main` tip was the *more* complete one this time, not the less: the fix
  commit `1641fa5f` itself has two `cancelled` check-runs ("Native engines
  (Rust)", "Tests (Python 3.12)") alongside eleven `success` ones (`gh api
  repos/2AMLogic/klayout-tools/commits/1641fa5f.../check-runs`), while `main`
  tip's own fourteen check-runs are all `conclusion: "success"` with none
  `cancelled`/`queued`/`null` (checked live at pin-selection time, not
  assumed from the issue's filing-time snapshot, which had recorded `main`
  as *not yet* fully green). Per the documented precedent (pin-history entry
  4), the newest candidate whose own CI is fully `success` is the correct
  pin — that is `main` tip here, one commit past the fix itself.
- **Result**: **The pin bump is a no-op for the four already-committed
  plans' drawn geometry; `differential_driver` now has a committed,
  correctly-drawn plan; and the pin bump's new mismatch-warning check found
  a real, pre-existing device-class discrepancy in three of the four
  already-committed plans that this record does not fix.**

  | Block | Superseded run (`klt 0.4.0+gbf90e1d5e3a6`) | This run (`klt 0.4.0+g61e743088f47`) |
  |---|---|---|
  | `differential_receiver` | 11 groups, 1/8 nets (`VDD`), 0 violations | unchanged — `cmp`-identical GDS; **1 new `warnings[]` entry** (see item 3) |
  | `se_receiver_dm` | 13 groups, 1/9 nets (`VDD`), 0 violations | unchanged — `cmp`-identical GDS; **3 new `warnings[]` entries** |
  | `se_receiver_dp` | 13 groups, 1/9 nets (`VDD`), 0 violations | unchanged — `cmp`-identical GDS; **3 new `warnings[]` entries** |
  | `dplus_pullup` | 24 groups, 6/21 nets, 0 violations | unchanged — `cmp`-identical GDS; 0 `warnings[]` (its `res_array` groups declare plain `PPOLYF_U`, matching the default-flavor device they draw — no mismatch) |
  | `differential_driver` | no committed plan — throwaway probe: 12 groups / 0/10 nets / DRC-clean / wrong device on 2 groups | **plan committed**: 12 groups / 0/10 nets / DRC-clean / **0 `warnings[]`** — both `RM1` groups confirmed to draw the correct metal-1 device |

  1. **Byte-identical confirmation for the four already-committed plans is
     direct, not inferred from an unchanged summary.** `cmp` against the
     superseded record's frozen `*.gds` artifacts
     (`verification/records/analog-layout/artifacts/20260912-191922-5953e89/`)
     reports zero differences for all four
     (`differential_receiver.gds`, `dplus_pullup.gds`, `se_receiver_dm.gds`,
     `se_receiver_dp.gds`). The DRC deck's own `content_hash`
     (`sha256:3c2f4e4b…`) is also unchanged from the superseded record —
     the curated `gf180mcu` deck's rule content did not move with this pin,
     only `gen.py`/`layout_plan_execute.py`'s Python did.
  2. **`differential_driver` now has a committed, correctly-drawn plan.**
     `layout/analog/plans/differential_driver.json` (added by this record)
     is structurally identical to the superseded record's throwaway probe
     — 12 `device_groups[]` entries (8 MOS `mos_array` + 2 `PPOLYF_U`
     `res_array` slew resistors + 2 `RM1` `res_array` series resistors), one
     row, `routing.width_um: 0.23` — with exactly one addition:
     `params.metal_level: 1` on the two `RM1` groups (`r_serdm`, `r_serdp`).
     That one field is the entire fix's payload for this block: with it,
     `klt gen` draws real `Metal1`/`Via1`/`Metal2` geometry for both 800 µm
     × 2 µm series-termination resistors instead of the silently-substituted
     `ppolyf_u` poly body the superseded record caught (see "Run
     configuration" above for the layer-level confirmation). Execution
     places all 12 groups (bbox 1643.76 × 61.82 µm), routes 0 of 10 nets
     (`unrouted_nets`: `DM_GATE`, `DM_OUTINT`, `DM_PREDRV`, `DP_GATE`,
     `DP_OUTINT`, `DP_PREDRV`, `TXDM`, `TXDP`, `VDD`, `VSS` — the block's
     internal nodes and supplies), and `klt drc` reports clean (0
     violations). This is consistent with every other block's first
     measurement (`differential_receiver`/`se_receiver_dm`/`se_receiver_dp`
     each started 0/N routed; `dplus_pullup` started 0/21 before issue #62's
     grouping work raised it to 6/21) — placed-but-unrouted is this repo's
     normal starting state for a newly-committed plan, not a red flag.
     `scripts/gen_analog_layout.py`'s `BLOCKED_BLOCKS` is now empty; all
     five analog blocks execute from a committed plan.
  3. **A pre-existing, previously-undetectable device-class mismatch is now
     visible in three of the four already-committed plans, and this record
     does not fix it — it is out of this issue's scope and is filed
     separately.** `differential_receiver.json` (`r_bias`), `se_receiver_dm.
     json` (`r_bias`, `r_1`, `r_2`), and `se_receiver_dp.json` (`r_bias`,
     `r_1`, `r_2`) each declare a `res_array` group with `device_class:
     "PPOLYF_U_1K"` — matching the netlist, which genuinely instantiates
     those bias/reference resistors as `ppolyf_u_1k` (a specific 1 kΩ/sq
     poly-resistor flavor: `design/netlist/se_receiver_dm.spice`'s `XR1`/
     `XR2`/`XRBIAS`, for example) — but none of the seven affected groups
     sets `params.flavor: "1k"`. `res_array`'s default flavor is
     `"generic"`, a *different* sheet-resistance body
     (`gen._DEFAULT_RES_FLAVOR`/`_PDK_RES_FLAVOR_DEVICE_CLASS["gf180mcu"]`:
     `"generic"` → `"ppolyf_u"`, `"1k"` → `"ppolyf_u_1k"`), so every one of
     these seven groups has been drawing the wrong poly-resistor flavor
     since whichever run first committed its plan (`differential_receiver`/
     `se_receiver_dm`/`se_receiver_dp`: 2026-09-05, issue #65's `routing`
     fix; the `device_class` values themselves predate that). This is the
     *exact same shape* of bug `differential_driver`'s `RM1` groups had —
     declared `device_class` and resolved generator output silently
     disagreeing — just on `flavor` instead of `metal_level`, and on
     already-committed plans instead of a not-yet-committed one. It was not
     visible before this pin move because `_device_class_mismatch_warnings`
     (PR #1774, this pin's own payload) did not exist yet; the GDS these
     three plans produce is unchanged by this pin move (item 1), so this is
     not a new defect introduced here — it is a real, standing one this pin
     move's diagnostics newly reveal. Per `CLAUDE.md`'s "no claim without a
     testbench" and this repo's scope-discipline convention, fixing three
     already-committed, DRC-clean plans is real per-block layout work (the
     correct `1k`-flavor geometry needs its own DRC/LVS re-verification, and
     `differential_driver`'s own fix in this same record shows that is not
     a one-line change to *validate*, even though the plan-side fix itself
     is one field per group) — out of scope for this issue, which is
     specifically the `klt` re-pin and `differential_driver`'s plan. Filed
     as a new issue in this repo (not upstream — `klt`'s new warning is
     working exactly as designed; the bug is in this repo's own plan
     documents) — see "Links".
  4. **A minor, immaterial packaging nuance**: this run's harness reports
     `klt_version: "0.4.0"` (no `+g<hash>` local segment) in
     `analog-layout-report.json`, where every prior record's harness output
     carried the git-suffixed form. `klt --version` (the CLI) still reports
     the full `klt 0.4.0+g61e743088f47` — this record uses that value for
     its own `provenance.klt_version`, consistent with every prior record.
     The difference traces to `klayout_tools.__version__` (an
     `importlib.metadata`-derived attribute) vs. the CLI's own
     git-describe-derived string, not to anything this pin move changed
     about `gen_analog_layout.py`, `layout_plan_execute.py`, or `gen.py`;
     not investigated further as immaterial to every claim in this record.
- **klt provenance**: `klt` 0.4.0+g61e743088f47 @ klayout-tools
  `61e743088f4722854d18b0cc48bd98c3dee58cf6` (this repo's newly pinned
  `KLT_REV`, provisioned into a from-scratch `.venv` by
  `scripts/setup-env.sh` for this run), `klayout` 0.30.12; PDK `gf180mcuD`,
  open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (unchanged); DRC deck
  `gf180mcu`, content hash
  `sha256:3c2f4e4be2524a0fe964cd1b2c4c171c0f904586c10eaa8c4ffe3511909e4706`
  (unchanged from the superseded record), `released: false`. klt drove
  every leg of this run (plan execution for all five blocks, DRC, and the
  direct GDS-layer/`resolved_params`/`warnings[]` checks on
  `differential_driver`); nothing here is hand-derived.
- **Links**:
  - Driver: `scripts/gen_analog_layout.py` (`BLOCKED_BLOCKS` emptied +
    module docstring updated; no behavior change to plan execution/DRC for
    the four previously-executed blocks)
  - Changed input: `scripts/setup-env.sh` (`KLT_REV` only)
  - Pin history entry: `docs/environment-setup.md` § "Pinned versions",
    move 5
  - New committed plan: `layout/analog/plans/differential_driver.json`
    (issue #72)
  - Plans under test, unchanged, byte-identical to the superseded record:
    `layout/analog/plans/differential_receiver.json`,
    `layout/analog/plans/dplus_pullup.json`,
    `layout/analog/plans/se_receiver_dm.json`,
    `layout/analog/plans/se_receiver_dp.json`
  - Designs under test: `design/netlist/*.spice` — none changed
  - Raw artifacts:
    `verification/records/analog-layout/artifacts/20260913-192744-b653c71/`
    — `analog-layout-report.json` (the combined report); one
    `<block>-layout-plan.json`, `<block>.gds` and `<block>-drc-full.json`
    per executed block, now including `differential_driver` for the first
    time as a real committed-plan execution rather than a throwaway probe.
  - Upstream fix consumed: klayout-tools#1731 (tracked by this repo, closed
    via PR #1774, merge `1641fa5f`) — `res_array` drawn-geometry support for
    gf180mcu's `rm1`/`rm2`/`rm3` metal-resistor device classes, plus the
    `device_groups[]`-declared-class-vs-resolved-generator-output mismatch
    warning.
  - New finding, filed as a repo-local issue (not upstream — this is this
    repo's own plan documents disagreeing with the netlist they were
    written against, not a `klt` gap): the `PPOLYF_U_1K`/default-`"generic"`
    `flavor` mismatch on `differential_receiver.json`'s `r_bias` and
    `se_receiver_dm.json`/`se_receiver_dp.json`'s `r_bias`/`r_1`/`r_2` groups
    (see "Result" item 3). Tracking issue:
    [gf180-usb2-phy#73](https://github.com/2AMLogic/gf180-usb2-phy/issues/73).
  - Comments left on #53 and #51 pointing at this record.
  - Superseded: `verification/records/analog-layout/records/20260912-191922-5953e89.md`
    — same four-block claim for the drawn-geometry no-op; superseded because
    the `klt` pin (and therefore `differential_driver`'s plannability and
    the mismatch-warning visibility for the other three blocks) changed.
- **Timestamp / author**: 2026-09-13T19:27:44Z, Loom Builder (AI agent),
  issue #72.
- **Supersedes**: 20260912-191922-5953e89 — same four-block drawn-geometry
  claim, same driver, same four plan documents and design sources
  (byte-identical GDS confirmed by direct `cmp`). The changed inputs are
  `scripts/setup-env.sh`'s `KLT_REV`, `scripts/gen_analog_layout.py`'s
  `BLOCKED_BLOCKS`, and the new `differential_driver.json` plan — exactly
  why that record's `differential_driver` "no committed plan" finding, and
  its silence on the other three plans' `warnings[]`, are now superseded by
  this one.
