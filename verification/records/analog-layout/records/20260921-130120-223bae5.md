<!-- record-meta
{
  "record_id": "20260921-130120-223bae5",
  "experiment": "analog-layout",
  "supersedes": "20260914-023747-74372a3",
  "git_revision": "223bae56c85af608e12d562c885d32ce3c7e7edf",
  "provenance": {
    "klt_version": "0.5.0+ge8ca621a6961",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637",
      "released": false
    },
    "inputs": [
      { "path": "scripts/setup-env.sh", "content_hash": "sha256:927faac92825021c10dcf6dde027589ccc4110ef41da2f286f59f9e36f12af80" },
      { "path": "scripts/gen_analog_layout.py", "content_hash": "sha256:ff45c4e289284df5c9427f4884fc2b446acd57440a685ac8ae24ec7b75c3865d" },
      { "path": "layout/analog/plans/differential_receiver.json", "content_hash": "sha256:77c62e64b32eeb81732dedd2ec841a6e584aa64bcebbd598cd0586081694abdf" },
      { "path": "layout/analog/plans/dplus_pullup.json", "content_hash": "sha256:0f7dac6b6c74df0a171b57d8c7df20faf943eb9a8be12d9a2fc6d88e8c68fa31" },
      { "path": "layout/analog/plans/se_receiver_dm.json", "content_hash": "sha256:90b3ec9e6b22ce691540746afc85731e3d9692c94a63028167049b9afbf5a687" },
      { "path": "layout/analog/plans/se_receiver_dp.json", "content_hash": "sha256:4806a6fa835c72f6205d1aef2a77732703f109205bac472a76bfb6b1b961a81a" },
      { "path": "layout/analog/plans/differential_driver.json", "content_hash": "sha256:8d134f5e80e13342878b9e47c3f9670595c3e5f7b7cc842eee114484bfd896c6" },
      { "path": "design/dplus_pullup.sch", "content_hash": "sha256:81eaf5101b343bc5773f7d2800ea3dec40f8d7a5388240b8d22fd13a0b7684e6" },
      { "path": "design/netlist/differential_driver.spice", "content_hash": "sha256:bd4b2608c986fd1e305be7b1522e94f578cf36206fe30c13325f48a55f46afa5" },
      { "path": "design/netlist/differential_receiver.spice", "content_hash": "sha256:b8b230603cfc88b05ebe27eb18e476ddbbfa0cfab64c23677b2862ae8c658e0e" },
      { "path": "design/netlist/dplus_pullup.spice", "content_hash": "sha256:9c645ecf0da6d80fb05d85c33d2941cc48bc328a00cd427f0c11bdf980b2cceb" },
      { "path": "design/netlist/se_receiver_dm.spice", "content_hash": "sha256:ebfc91ac1306060dfa0ffce7d91e048b4fc8b50cfebbd5804c3aaf9b58a20554" },
      { "path": "design/netlist/se_receiver_dp.spice", "content_hash": "sha256:1f854210680331a1df73b96b317479cad4af325192fec68b0b61fb1421fac0b3" }
    ]
  }
}
-->

# Record 20260921-130120-223bae5

- **Record ID**: 20260921-130120-223bae5
- **Claim**: re-executes all five committed analog layout plans with every
  design input byte-identical to the superseded record — the ten
  `provenance.inputs[]` design/plan hashes above match
  `20260914-023747-74372a3`'s exactly; the **only** changed input is
  `scripts/setup-env.sh` (this repo's `klt` re-pin to
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`, 263 commits forward — see
  `docs/environment-setup.md`'s re-pin history, move 6). This is the
  re-measure that re-pin discipline exists to force: does the standing
  analog-layout state — five plans placed, four blocks drawing
  flavor-correct resistor bodies, `0 warnings[]` everywhere, DRC-clean
  everywhere, the routed-net tally `0/10`, `1/8`, `6/21`, `1/9`, `1/9` —
  survive 263 upstream commits unchanged? **Four of the five blocks:
  byte-for-byte yes. `dplus_pullup`: routing redrawn (geometry, not
  status) — details below. Routed/unrouted counts, warning counts,
  DRC verdicts, device-group counts and bboxes are unchanged on all
  five.**
- **Design provenance**: schematic — `design/*.sch` exported to
  `design/netlist/*.spice` by `design/netlist.py`; every netlist hash
  identical to the superseded record's. No design source changed.
- **Run configuration**: `PDK_ROOT=$HOME/.volare .venv/bin/python
  scripts/gen_analog_layout.py --format json`, from a venv provisioned
  fresh in this worktree by `./scripts/setup-env.sh` at the **new**
  `KLT_REV` (`e8ca621a…`), plus a direct
  `klt drc --deck gf180mcu --format json` per executed block (all five),
  matching the superseded record's convention. `klayout` 0.30.10 — the
  version the new pin reports as tested (`klt version --format json`'s
  `klayout_version_expected`), installed into the venv explicitly so
  `klayout_version_mismatch` is `false` on every envelope below.
- **Statistical convention**: N/A — deterministic re-execution of fixed
  plans against fixed netlists. Per-block DRC verdicts re-derived from
  scratch, not transplanted.
- **Result**: **the pin move alone does not move any recorded analog
  status** — with one real, scoped geometry change on `dplus_pullup`'s
  drawn routing:

  | Block | Execution | DRC | `warnings[]` | Geometry vs `20260914-023747-74372a3` |
  |---|---|---|---|---|
  | `differential_driver` | 12 groups placed, 0/10 nets routed | `clean`, 0 violations | 0 | **`cmp`-identical** |
  | `differential_receiver` | 11 groups, 1/8 routed (`VDD`) | `clean`, 0 | 0 | **`cmp`-identical** |
  | `dplus_pullup` | 24 groups, 6/21 routed | `clean`, 0 | 0 | **routing re-drawn** — per-layer Boolean XOR vs the superseded artifact is zero on every layer except `34/0` (Metal1): 610,832,320 → 547,328,320 dbu² (63,504,000 dbu² less drawn metal); see below |
  | `se_receiver_dm` | 13 groups, 1/9 routed (`VDD`) | `clean`, 0 | 0 | **`cmp`-identical** |
  | `se_receiver_dp` | 13 groups, 1/9 routed (`VDD`) | `clean`, 0 | 0 | **`cmp`-identical** |

  All bboxes match the superseded record to the µm.

  **What changed on `dplus_pullup`, and why.** The plan and netlist are
  byte-identical, so this is upstream router behavior that moved with the
  pin, observable in three independent places:

  1. `route_length_um` on four already-routed nets collapsed:
     `NODE2` 227.100 → 75.486, `NODE3` 233.950 → 76.171, `NODE4`
     247.670 → 77.543, `NODE5` 454.794 → 65.166 µm — consistent with the
     lost Metal1 area above (less far-way wire drawn, same nets routed).
  2. Per-leg rejection reasons in the plan response gained a new, more
     precise shape — "crosses *N* µm of block *X*'s interior on the side
     **away** from its own pin *P* (which faces *θ*); that pin's edge
     margin only describes the approach on the side it faces" — replacing
     the old blanket "crosses through its own pin's block" wording, plus
     new per-leg `landed_on_block` / `channel_track` fields.
  3. Backbone challenges on the `mp_sw*` ladder pick up more stuck pins
     (e.g. `NODE2`: "1 of 22 pins could not be connected" → "10 of 22"),
     i.e. the refined rejection model declines legs the previous model
     accepted.

  These are the documented routing-semantics commits of
  klayout-tools#1931 ("reject a leg that reaches a pin from the side it
  doesn't face"), #1936 ("hold a leg's own-block approach to the deck's
  spacing rule"), #1945 (ring-gap clearance/allowance), #2098 ("isolate
  routing passes and publish distinct final DRC counts") and #2144
  ("floor routed metal against each layer's min-area rule") — all merged
  between the two pins, all in the routing/composition path. **None of
  them flips any verdict this repo records**: the block stays DRC-clean,
  the routed-net tally stays 6/21, the 15 unrouted nets stay unrouted
  (still tracked under "the actual critical path" in `layout/README.md`
  § Analog (d), and unchanged by this move), `warnings[]` stays 0, the
  bbox stays put. The re-drawn Metal1 is a **shorter** routing of the
  same six nets, not a reconnection of any of the fifteen that failed
  before. Whether the new leg model routes a *future* version of this
  plan better or worse is a design question for the per-block layout
  work, not a defect this record fixes.
- **klt provenance**: `klt` 0.5.0+ge8ca621a6961 @ klayout-tools
  `e8ca621a6961879cec1af60cc932c3b3d58ddcaa`; `klayout` 0.30.10
  (build-tested version, `klayout_version_mismatch: false` on every
  envelope); PDK `gf180mcuD`, open_pdks
  `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (unchanged); DRC deck
  `gf180mcu` content hash
  `sha256:95c2eb9148b29ded02182aeca4222287ff57f813691d5e2f9f8d7a675f6cb637`
  (changed from the superseded run's `sha256:3c2f4e4b…` — the deck ships
  with the tool and moved with the pin; every DRC verdict below was
  re-derived against the new deck, not carried over). Each per-block
  `klt drc` envelope carries `provenance.input.content_hash` pinning the
  exact GDS it graded (role `layout`).
- **Links**:
  - Driver: `scripts/gen_analog_layout.py` (hash byte-identical to the
    superseded record's)
  - Changed input: `scripts/setup-env.sh` — the move-6 `klt` re-pin
    (`docs/environment-setup.md`)
  - Raw artifacts:
    `verification/records/analog-layout/artifacts/20260921-130120-223bae5/`
    — `analog-layout-report.json` (the combined report); one
    `<block>-layout-plan.json`, `<block>.gds` and `<block>-drc-full.json`
    per executed block (all five)
  - Superseded record (kept, append-only):
    `verification/records/analog-layout/records/20260914-023747-74372a3.md`
  - Upstream routing-semantics commits named above —
    klayout-tools#1931, #1936, #1945, #2098, #2144
  - Tracking narrative: `layout/README.md` § "Analog" (eleventh run
    column)
  - Dispatching issue: [gf180-usb2-phy#78](https://github.com/2AMLogic/gf180-usb2-phy/issues/78)
    (the T1 signoff-manifest era whose grading requires the new pin)
- **Timestamp / author**: 2026-09-21T13:01:20Z, Loom Builder (AI agent),
  issue #78.
- **Supersedes**: 20260914-023747-74372a3 — same five-plan claim at the
  same input hashes; superseded mechanically because its one remaining
  fresh input (`scripts/setup-env.sh`) changed with the re-pin, and its
  verdicts are reconfirmed here rather than assumed. Its flavor-fix
  narrative (the tenth run) remains the standing account of *that* change;
  nothing here contradicts it.
