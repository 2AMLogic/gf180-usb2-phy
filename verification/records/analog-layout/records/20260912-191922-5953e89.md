<!-- record-meta
{
  "record_id": "20260912-191922-5953e89",
  "experiment": "analog-layout",
  "supersedes": "20260909-215500-29c5780",
  "git_revision": "5953e8930d93a4acac07822d48ec3e21c01ec713",
  "provenance": {
    "klt_version": "0.4.0+gbf90e1d5e3a6",
    "pdk": {
      "name": "gf180mcuD",
      "version": "open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b"
    },
    "deck": {
      "name": "gf180mcu",
      "content_hash": "sha256:3c2f4e4be2524a0fe964cd1b2c4c171c0f904586c10eaa8c4ffe3511909e4706"
    },
    "inputs": [
      { "path": "scripts/setup-env.sh", "content_hash": "sha256:e3c14a124e30860548552a4b1de872b3732422184ea41f66818c23c0c16567b6" },
      { "path": "scripts/gen_analog_layout.py", "content_hash": "sha256:2ce20364edf04a292c486c7eb32c23b8a21f38ab7e5111066a45581fc14ed630" },
      { "path": "layout/analog/plans/differential_receiver.json", "content_hash": "sha256:f04c6d416066179975458c7c253db9f6143b3f0e1a98cb4519692a9e5871de1c" },
      { "path": "layout/analog/plans/dplus_pullup.json", "content_hash": "sha256:0f7dac6b6c74df0a171b57d8c7df20faf943eb9a8be12d9a2fc6d88e8c68fa31" },
      { "path": "layout/analog/plans/se_receiver_dm.json", "content_hash": "sha256:4f8d5b08e4007e851fa1095528b40bb926c4fe44864af8cb886472335b810b36" },
      { "path": "layout/analog/plans/se_receiver_dp.json", "content_hash": "sha256:4d16672ea830a0d695ee62edab0b713046854aedc873f9ba67c57152869cec8b" },
      { "path": "design/dplus_pullup.sch", "content_hash": "sha256:81eaf5101b343bc5773f7d2800ea3dec40f8d7a5388240b8d22fd13a0b7684e6" },
      { "path": "design/netlist/differential_driver.spice", "content_hash": "sha256:bd4b2608c986fd1e305be7b1522e94f578cf36206fe30c13325f48a55f46afa5" },
      { "path": "design/netlist/differential_receiver.spice", "content_hash": "sha256:b8b230603cfc88b05ebe27eb18e476ddbbfa0cfab64c23677b2862ae8c658e0e" },
      { "path": "design/netlist/dplus_pullup.spice", "content_hash": "sha256:9c645ecf0da6d80fb05d85c33d2941cc48bc328a00cd427f0c11bdf980b2cceb" },
      { "path": "design/netlist/se_receiver_dm.spice", "content_hash": "sha256:ebfc91ac1306060dfa0ffce7d91e048b4fc8b50cfebbd5804c3aaf9b58a20554" },
      { "path": "design/netlist/se_receiver_dp.spice", "content_hash": "sha256:1f854210680331a1df73b96b317479cad4af325192fec68b0b61fb1421fac0b3" }
    ]
  }
}
-->

# Record 20260912-191922-5953e89

- **Record ID**: 20260912-191922-5953e89
- **Claim**: Moving this repo's `klt` pin from
  `8bd3f0b09a87641b093d655a7282b8f730db19e8` (`klt 0.4.0+g8bd3f0b09a87`) to
  `bf90e1d5e3a624ea2e098efdfe76596081508751` (`klt 0.4.0+gbf90e1d5e3a6`) — a
  commit chosen specifically because it has klayout-tools PR #1662's merge
  (`df36f4fce8c63e367c132a3335369a877acc4ebb`, "recognise gf180mcu's drawn
  metal-resistor family") as a strict ancestor — has two effects, and they
  are of different kinds. **For the four already-committed plans (with
  every plan document and design netlist byte-identical to the superseded
  record), the pin bump is a no-op**: identical `bbox_um`, identical
  routed-net counts (1/8, 1/9, 1/9, 6/21), identical DRC-clean status, and
  `cmp`-identical GDS files. **For `differential_driver`, `klt`'s netlist
  digest builder now resolves the netlist cleanly for the first time** —
  the `rm1` device-class-unknown error that blocked every prior record is
  gone, confirmed live, not assumed. That is real progress, and it is also
  not the same thing as "a plan can now be committed": a throwaway probe
  plan run against the newly-ingestible netlist shows that `klt gen`'s
  `res_array` generator has no way to draw a gf180mcu metal-1 resistor at
  all, and — the sharper finding — silently draws the *wrong* device (a
  poly resistor) instead of failing when a plan leaves `flavor`/
  `metal_level` at their defaults for an `rm1`-classed device, with no
  error and no warning. `differential_driver` therefore still has no
  committed plan, for a new and much narrower reason than before.
- **Design provenance**: schematic — `design/*.sch` exported to
  `design/netlist/*.spice` by `design/netlist.py`. **No design source and no
  plan document changed relative to the superseded record**: all five block
  netlists, `design/dplus_pullup.sch`, and all four `layout/analog/plans/*.json`
  carry content hashes byte-identical to `20260909-215500-29c5780`'s (compare
  the `inputs[]` arrays directly). The changed inputs are
  `scripts/setup-env.sh` (`KLT_REV` only) and `scripts/gen_analog_layout.py`
  (the `BLOCKED_BLOCKS` reason string + a probe-message wording fix for
  `differential_driver`, described under "Links" below — no behavior change
  to plan execution, DRC, or the four executed blocks, confirmed by the
  byte-identical GDS/report comparison in "Result").
- **Run configuration**: `PDK_ROOT=$HOME/.volare .venv/bin/python
  scripts/gen_analog_layout.py --format json` from a **freshly created**
  `./scripts/setup-env.sh` venv (`rm -rf .venv` first) at the new `KLT_REV`,
  plus a direct `klt drc --deck gf180mcu --format json` per executed block.
  `npm run check:ci` was run at the new pin **before** the pin was
  committed and passed (see "Links"). For `differential_driver`, three
  additional direct probes (none committed as a plan) established the
  claim above:

  1. `klayout_tools.netlist_digest.build_netlist_digest(
     "design/netlist/differential_driver.spice", top="differential_driver",
     form="subckt-call", deck="gf180mcu")` — the exact entry point
     `_probe_blocked()` and `klt layout-plan`/`klt lvs` share — now returns a
     12-device digest (8 MOS + 2 `PPOLYF_U` slew resistors + 2 `RM1` series
     resistors) instead of raising.
  2. A throwaway, uncommitted `klt.layout_plan.request/1` document (one
     `device_groups[]` entry per device, single row, `routing.width_um:
     0.23` — the same mechanical shape the three receiver plans use, no
     per-block layout design work) was driven through
     `execute_layout_plan_document`. It **succeeds**: 12 device groups
     place, 0/10 nets route (net names are the block's internal nodes;
     `unrouted_nets`: `DM_GATE`, `DM_OUTINT`, `DM_PREDRV`, `DP_GATE`,
     `DP_OUTINT`, `DP_PREDRV`, `TXDM`, `TXDP`, `VDD`, `VSS`), `klt drc`
     reports clean (0 violations). This is consistent with every other
     block's first measurement (see `20260818-113513-89520ac` and
     `20260905-190024-525c67c`) — a newly-ingestible block starts placed and
     un-routed, not routed.
  3. **The device drawn for the two `RM1`-classed groups is not an `RM1`
     device.** The probe plan's `r_serdm`/`r_serdp` groups declare
     `"device_class": "RM1"` (which resolves correctly against the digest —
     `layout_plan.py`'s own validation only checks that a `(name,
     device_class)` pair matches a real digest entry, never that the
     generator draws that class) and `"generator": "res_array"` with no
     `flavor`/`metal_level` override. Reading `klt gen`'s own source
     (`gen.py`'s `_PDK_RES_FLAVOR_LAYERS` and `_PDK_METAL_RES_LEVELS`
     tables, not inferred): gf180mcu's only `res_array` flavours are
     `generic`/`1k`/`2k`/`3k`, all poly-body, and `_PDK_METAL_RES_LEVELS`
     (the table `metal_level` reads) has entries for exactly one PDK family,
     which is not gf180mcu. With no override, `res_array` resolves
     `flavor="generic"` and draws a `ppolyf_u` poly resistor — confirmed
     directly against the response's `resolved_params`
     (`{"length_um": 800.0, "width_um": 2.0, "num": 1, "dummy": 0}`, the
     `res_array` PCell's own poly-body parameter set, not any metal-layer
     one) — silently, with an empty `warnings[]` list and no error. Two
     confirmatory probes rule out any ambiguity: explicitly requesting
     `"flavor": "rm1"` hard-rejects with `generator 'res_array':
     params.flavor 'rm1' is not a recognised poly-resistor flavour for PDK
     family 'gf180mcu' -- supported flavours: 1k, 2k, 3k, generic`, and
     explicitly requesting `"metal_level": 1` hard-rejects with `generator
     'res_array': params.metal_level requires a PDK family with a drawn
     metal-resistor device class -- supported families: sky130`. So `klt`
     *can* hard-reject an unsupported resistor request when the request is
     explicit about what it wants — it just does not do so for the silent,
     default-flavor path that a plan naming `device_class: "RM1"` and
     omitting `flavor`/`metal_level` entirely takes.
- **Statistical convention**: N/A — deterministic runs against fixed
  netlists and fixed (or, for `differential_driver`, explicitly
  throwaway/uncommitted) plan documents.
- **Pin ancestry verified, not assumed.** `gh api
  repos/2AMLogic/klayout-tools/compare/df36f4fce8c63e367c132a3335369a877acc4ebb...bf90e1d5e3a624ea2e098efdfe76596081508751`
  reports `status: "ahead"`, `ahead_by: 39`, `behind_by: 0` — PR #1662's
  merge commit is a strict ancestor of the new pin. The new pin is not
  klayout-tools' bare `main` tip at measurement time (`160073f3...`, whose
  own CI was still `queued` on several legs when checked); `bf90e1d5...` was
  chosen instead because every one of its own CI check-runs
  (`Tests (Python 3.10/3.11/3.12/3.13)`, both `Native engines (Rust)` legs,
  `Lint (ruff)`, `site/ (tsc + vitest)`, the `klt verify` smoke test) had
  already completed with `conclusion: "success"` at check time — the same
  "confirm CI, don't just take `main`" bar this repo's pin moves have used
  before. The old pin `8bd3f0b0` is `behind_by: 0` / `ahead_by: 1` relative
  to the new one (one commit apart, `bf90e1d5...` itself is the very next
  commit after the old pin on `main`), so this is a strict fast-forward.
- **Result**: **The pin bump is a no-op for the four already-committed
  plans, and lands the ingestion half of `differential_driver`'s blocker —
  but not the half that would let a plan be committed.**

  | Block | Superseded run (`klt 0.4.0+g8bd3f0b09a87`) | This run (`klt 0.4.0+gbf90e1d5e3a6`), plans byte-identical |
  |---|---|---|
  | `differential_receiver` | 11 groups, 1/8 nets (`VDD`), 0 violations | unchanged — `cmp`-identical GDS |
  | `se_receiver_dm` | 13 groups, 1/9 nets (`VDD`), 0 violations | unchanged — `cmp`-identical GDS |
  | `se_receiver_dp` | 13 groups, 1/9 nets (`VDD`), 0 violations | unchanged — `cmp`-identical GDS |
  | `dplus_pullup` | 24 groups, 6/21 nets, 0 violations | unchanged — `cmp`-identical GDS |
  | `differential_driver` | ingestion failed (`rm1` unknown device class) | **ingestion succeeds** (12-device digest resolves); **no committed plan** — a throwaway probe places 12 groups / 0/10 nets routed / DRC-clean, but two of those groups silently draw the wrong device (see above) |

  1. **The deck itself changed content, and that is expected, not a
     regression.** `klt drc`'s own `provenance.deck.content_hash` moved from
     `sha256:79e71a1e…` to `sha256:3c2f4e4b…`, and `provenance.deck.released`
     flipped from `true` to `false` — both consistent with PR #1662 editing
     `klayout_tools/decks/gf180mcu.py` (the file the merged PR's own file
     list names) to add the `rm1`/`rm2`/`rm3` (and `tm6k`/`tm9k`/`tm11k`/
     `tm30k`) resistor device classes, and with the new pin sitting past
     klayout-tools' most recent tagged release. The four executed blocks'
     `klt drc` output is otherwise unchanged: `status: "clean"`,
     `violation_count: 0`, and `rule_counts: {}` for all four, matching the
     superseded record exactly; the only observable difference in the raw
     `*-drc-full.json` artifacts is two additional named entries in
     `coverage.rules_skipped` (`metal4.space.1`, `metal4.width.1`) and the
     deck hash/`released` fields themselves — no new violation, no changed
     `layers_checked` set, no geometry difference (GDS files are `cmp`-
     identical to the superseded record's).
  2. **`differential_driver`'s ingestion blocker is resolved, and this is a
     narrower fix than "differential_driver can now be laid out."**
     klayout-tools#1640 (the tracking issue this repo had open against
     klayout-tools for the missing metal-resistor device class) is
     satisfied at the *extraction* layer — `netlist_digest`, the same code
     `klt lvs`'s reference-side ingestion and `klt layout-plan`'s Phase B
     validation both call, now resolves `rm1` cleanly. It does not follow,
     and this record does not claim, that `klt gen` can *draw* one: the
     probe above shows `res_array`'s generator-side support was not part of
     PR #1662's scope, and the deferred-gap prose already in
     `layout_plan.py` itself says as much ("the generator-side device-class
     mapping does not exist until Phase C compiles a group into a real `klt
     gen` call", "full shape-class consistency checking is deferred").
  3. **The silent-wrong-device finding is the sharper, newly-discovered
     result of this record**, and is why `differential_driver` is *not*
     simply given a committed plan here even though it is technically
     placeable: a plan whose `res_array` groups draw `ppolyf_u` poly
     resistors where the netlist and the plan's own `device_class` both say
     `rm1` is not an honest layout attempt for this block — it is
     electrically a different circuit at exactly its two most
     electrically-significant devices (the 800 µm series-termination
     resistors), and would fail `klt lvs` device-class matching outright
     if ever extracted and compared. Per this repo's "no claim without a
     testbench, no claim the evidence does not support" convention, that is
     not committed under `layout/analog/plans/`. The throwaway probe and its
     full response/GDS/DRC output are frozen under this record's
     `artifacts/` instead, exactly as `differential_driver-probe-*` (not
     `differential_driver.json` under `layout/analog/plans/`).
  4. **`BLOCKED_BLOCKS` is not emptied of `differential_driver`, and that is
     a deliberate deviation from issue #70's literal "no longer lists it"
     wording, made because the finding does not fit the binary the issue
     anticipated.** The issue's acceptance criteria were written expecting
     either "ingestion still fails" (record the error, file upstream) or
     "ingestion succeeds" (drop from `BLOCKED_BLOCKS`, commit a plan, report
     its initial routed state) — not a third state where ingestion succeeds
     but no *correct* plan can yet be authored for a different, narrower
     reason. Unconditionally removing the entry would either (a) require
     committing the wrong-device plan describe above, which this record
     declines to do, or (b) drop `differential_driver` out of
     `scripts/gen_analog_layout.py`'s per-run block count entirely with
     nothing replacing it — silently shrinking the report's own `total`
     from 5 to 4 blocks, which would let a future "4/4 fully routed,
     4/4 DRC-clean" run print `analog layout DELIVERED` while a whole block
     was never addressed. Per `CLAUDE.md`'s "no claim without a testbench"
     rule, that is worse than leaving the entry in place with a corrected
     reason. `scripts/gen_analog_layout.py`'s `BLOCKED_BLOCKS` entry and
     module docstring are updated in this same change to state the real,
     current blocker (see "Links"); `_probe_blocked()`'s per-run message is
     also corrected so it no longer claims "the blocker this entry records
     is gone" when ingestion succeeding is only half of what the entry now
     records.
- **klt provenance**: `klt` 0.4.0+gbf90e1d5e3a6 @ klayout-tools
  `bf90e1d5e3a624ea2e098efdfe76596081508751` (this repo's newly pinned
  `KLT_REV`, provisioned into a from-scratch `.venv` by
  `scripts/setup-env.sh` for this run), `klayout` 0.30.12; PDK `gf180mcuD`,
  open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (unchanged); DRC deck
  `gf180mcu`, content hash
  `sha256:3c2f4e4be2524a0fe964cd1b2c4c171c0f904586c10eaa8c4ffe3511909e4706`
  (changed from the superseded record's `sha256:79e71a1e…` — see Result
  item 1), `released: false`. klt drove every leg of this run (plan
  execution, DRC, and the three `differential_driver` probes); nothing here
  is hand-derived.
- **Links**:
  - Driver: `scripts/gen_analog_layout.py` (`BLOCKED_BLOCKS` reason text +
    `_probe_blocked()` message updated for `differential_driver`; no
    behavior change to plan execution/DRC for the four executed blocks)
  - Changed input: `scripts/setup-env.sh` (`KLT_REV` only)
  - Pin history entry: `docs/environment-setup.md` § "Pinned versions", move 4
  - Plans under test (all four unchanged, byte-identical to the superseded
    record): `layout/analog/plans/*.json`
  - Designs under test: `design/netlist/*.spice` — none changed
  - Raw artifacts:
    `verification/records/analog-layout/artifacts/20260912-191922-5953e89/`
    — `analog-layout-report.json` (the combined report); one
    `<block>-layout-plan.json`, `<block>.gds` and `<block>-drc-full.json`
    per executed block (`differential_receiver`, `se_receiver_dm`,
    `se_receiver_dp`, `dplus_pullup`); and, for `differential_driver`, the
    throwaway probe's own `differential_driver-probe-plan.json` (the
    uncommitted request document), `differential_driver-probe-layout-plan.json`
    (the full Phase C response), `differential_driver-probe.gds`, and
    `differential_driver-probe-drc.json` — none of which are, or are meant
    to be read as, a committed layout plan.
  - Upstream fix consumed: klayout-tools#1640 (tracked by this repo,
    closed via PR #1662, merge `df36f4f`) — netlist ingestion for
    gf180mcu's `rm1`/`rm2`/`rm3` metal-resistor device classes.
  - New upstream gap found and filed generically by this run:
    [klayout-tools#1731](https://github.com/2AMLogic/klayout-tools/issues/1731)
    — `res_array` has no drawn-geometry path for a family's own recognised
    metal-resistor device class, and (independently) silently draws the
    wrong device when a `device_groups[]` entry's declared `device_class`
    does not match what the resolved generator/flavor actually draws.
  - Superseded: `verification/records/analog-layout/records/20260909-215500-29c5780.md`
    — same four-block claim, same driver, same plans, same design sources;
    superseded because the `klt` pin (and therefore
    `differential_driver`'s measured ingestion state) changed.
- **Timestamp / author**: 2026-09-12T19:19:22Z, Loom Builder (AI agent),
  issue #70.
- **Supersedes**: 20260909-215500-29c5780 — same four-block claim, same
  driver, same plan documents and design sources (byte-identical GDS
  confirmed by direct `cmp`). The changed input is `scripts/setup-env.sh`'s
  `KLT_REV`, which is exactly why that record's `differential_driver`
  ingestion finding is now stale and this record exists.
